# CLAUDE.md
<!-- TOKEN BUDGET: Keep this file under 80 lines. No session logs, no completed module designs, no book registries here. Those live in separate files loaded on demand. Before adding anything, ask: does Claude need this every single query? If not, it belongs elsewhere. -->

## Project
Astrologer AI Agent with RAG — Vedic astrology + palmistry PDFs → OCR → embed → ChromaDB → LLM Q&A agent.

## Current Session Focus
**S82 CLOSED: palm page-range gate SHIPPED ON — one-call contract satisfied via Chroma where-clause (page_ref $gte/$lte as two $and clauses). Gate is a no-op on 6 of 8 measured features, corrective on head line + fingers. Stale range map corrected before ship. OCR corruption re-confirmed as a KNOWN class (S79/S80) and non-causal for fate/head line per S81's layer table. Repo history purged, pack 85.35 -> 10.84 MiB. Suite 3354/0/7/1xp, HEAD 0dee13f.**
<!-- UPDATE THIS every session. One line only. -->

## Locked Decisions
- **Tiebreaker principle** (memory #10) — when classical sources are genuinely fragmented, user-perceived correctness wins over single-source purity; applies to all contested decisions.
- **V1 scope** — LLM-generated interpretive Q&A is OUT; AstroSage paragraph is the interpretive surface (palm reading REMOVED from this surface, S71 — see V1 PALM DROPPED below); deterministic calculation-engine output is V1's only structured Q&A surface.
- **P2 order** (locked) — Gochara → Sade Sati → Muhurta engine → Ashtakoot/Mangal dosha → Shadbala → D7 → D10 (demoted); P3+ deferred.
- **Ephemeris consolidation** (Session 44 flagged, Session 52 CLOSED) — `helpers/ephemeris.py` (`sidereal_longitude`/`sidereal_position`/`EphemerisError`) is canonical for sidereal-standard `swe.calc_ut()` calls; attribute-style `swe` access (`import swisseph as swe`, never `from swisseph import calc_ut`) is a hard constraint — several tests monkeypatch the shared `swisseph` module object and rely on late attribute lookup. 11/13 originally-catalogued files fully migrated; 3 call sites stay direct-by-design (not debt): chesta_bala.py's `swe.ECL_NUT` call (not a planet id, outside the helper's contract), kala_bala.py's Ayana Bala loop (flags are actually sidereal-standard, but it feeds the Session 47 oracle-locked Kranti formula — too fragile to touch), and kala_bala.py's Yuddha-Bala loop (needs ecliptic latitude, unexposed by the helper — this gap does NOT justify extending the helper's API). combustion.py deliberately re-wraps `EphemerisError` as its own `RuntimeError` to preserve the planet NAME in the message (Layer C test asserts `match="Sun"`) — do not normalize this to bare unwrapped propagation.
- **Router refuse-heavy posture** (Session 44 lock) — calc_router.py requires >=2 keyword hits to route any domain (0.4 floor, 0.15 margin). Single-keyword questions are REFUSED BY DESIGN. Dogfood-observed misses ("career potential" → 1 hit, refused; "Are we compatible?" → tie, refused) are scorecard data for future `_STEM_MAP` tuning, not bugs. Tune only with Answer Scorecard evidence, not preemptive guesses.
- **Dasha tier = payload property** (Session 49) — current_dasha always TIER_2_RANGE in V1; boundary window selects reason wording only. Tier attaches to answer claims, not evaluation moment.
- **Hybrid router** (Session 50) — Stage 2 LLM classification fires only on Stage-1 confidence-floor/margin refusals; routes only on high; fails closed; built-module fastpath bypasses both stages for flagship deterministic domains.
- **Golden harness STAGE2_VARIABLE tag** (Session 50) — rows tagged STAGE2_VARIABLE carry expected cross-run variance; check calc_router_stage2.log before treating a flip as regression.
- **Golden set arudha_lagna coverage** (Session 60) — ledger now 21 rows (3 arudha_lagna added); frozen comparison baseline superseded by Session 61 (see below). `sulabh_arudha_q3_refusal_probe` locked expected_tier=REFUSAL, produced via the Stage-2 low-confidence fallback — MATCH_STAGE2 posture, monitored not asserted; if a future Stage-2 flip to marriage_compatibility ever produces a SUBSTANTIVE answer (not just a differently-routed REFUSAL), that is a product-quality escalation to design chat, not a silent reclassification.
- **Stage 2 layman-intent prompt expansion** (Session 61) — RESOLVES the Session 59/60 "arudha_lagna Stage 1 unreachable" carry-forward: fixed by expanding Stage 2's system prompt (layman glosses + example phrasings per domain, plus an explicit fortune-telling→`domain=none` instruction), NOT by loosening Stage 1 keywords/`_CONFIDENCE_FLOOR`/`_MARGIN` — converging Stage 2's classification quality is the locked remedy path, router-threshold tuning stays gated on Answer Scorecard evidence per the Session 44 lock. New tracked metric: layman reachability (`route_question()` router-layer probe, 12 layman phrasings, final route succeeds vs. REFUSAL) — S61 measured 7/12 rescued (pre-edit baseline 3/12); full phrasing set + per-row pre/post table lives in `diagnostics/latest_run.md`'s S61 records; re-measure after any future Stage 2 prompt or threshold change. Frozen comparison baseline: `diagnostics/golden_scorecard_20260711_045928.md` (match=8/match_stage2=9/known_gap=2/new_gap=0). `_KNOWN_GAPS` down to 2 entries (`sulabh_marriage_q10`, `sulabh_dasha_q15`, both independent structural/scope locks, unrelated to Stage 2 confidence); if `sulabh_career_q4`/`sulabh_marriage_q9` ever reappear as NEW_GAP in a future run, treat as SUSPECTED STAGE-2 VARIANCE FIRST — check `calc_router_stage2.log` before regression triage.
- **Upapada refusal economics** (Session 62) — status quo ratified. Deterministically-refusable unwired-construct questions may burn one live Stage 2 call to refuse; refusal keywords rejected (would reintroduce the S57-removed unbuilt-keyword anti-pattern and block the future upapada route), wiring-for-cost rejected as scope creep. Re-open only with dogfood call-volume cost data (S44 evidence-gate).
- **Refusal payload contract** (Session 62) — REFUSAL DomainAnswers carry `answer_payload={'user_message': str}` (formatter-owned presentation, `format_refusal`); `demotion_reason` remains the router's verbatim machine contract. Tests assert structure, never wording.
- **Diagnostics retention convention** (Session 62, resolves Session 61 carry-forward) — keep-forever = scorecards with a frozen-baseline/supersession header OR cited by a commit message / `latest_run.md` entry; routine verification re-runs are prunable once a newer frozen baseline supersedes them; pruning is manual and logged, never automated.
- **upapada_lagna V1 wiring** (Session 63) — 6th routed domain. Payload mirrors arudha_lagna (`upapada_sign`/`lagna_sign`/`lord`/`co_lord_deciding_step`, `TIER_1_EXACT`). Two-source ratification: S57 JHora dual-confirmed capture (full §15.5.1 co-lord cascade) -> Aquarius/Ketu/step_2 for Sulabh, plus the S63 pipeline smoke test matching exactly. Golden row `sulabh_arudha_q3_refusal_probe` re-ratified `TIER_1_EXACT`/Stage-1 deterministic floor (was `REFUSAL`), `domain` field corrected `arudha_lagna` -> `upapada_lagna` (id kept for scorecard-history correlation). Frozen comparison baseline: `diagnostics/golden_scorecard_20260711_112836.md` (`match=9/match_stage2=8/known_gap=2/new_gap=0`), supersedes `golden_scorecard_20260711_045928.md`.
- **q10 routing shift ruled correct-classification improvement** (Session 63) — the upapada_lagna Stage 2 gloss's negative instruction ("couple compatibility... is marriage_compatibility") raised `sulabh_marriage_q10`'s question ("What does our overall compatibility mean for us as a couple?") from `medium` to `high` confidence (5/5 live probe) -> now routes `marriage_compatibility` instead of REFUSAL. Ruled correct, NOT a bug: it's a genuine couple-compatibility question. NO gloss edit made. `expected_tier` stays `TIER_4_INTERPRETIVE`, `KNOWN_GAP` continues to track only the unbuilt Tier 4 interpretive-synthesis depth, never the routing outcome. Re-open only if a semantically WRONG routing (not a confidence improvement on a genuinely correct classification) is ever observed.
- **muhurta_window V1 wiring** (Session 64) — 7th routed domain, and the pipeline's first and only `TIER_3_MUHURTA` domain: per-window Chandrabala/Tarabala/Panchaka composite over a fixed 7-day scan, genuinely dated per-window claims that don't fit the T1/T2 "tier = payload property" framing every other domain shares. Also the first WALL-CLOCK-ANCHORED domain — `build_domain_profile()`'s `evaluated_at_jd` is genuinely load-bearing (the scan window's own start_jd), not accepted-but-unused like arudha/upapada/av_transit; `test_orchestrator_muhurta.py`'s Layer C is STRUCTURAL only for this reason (window boundaries vary run to run by design, never byte-compared against the pinned-JD Layer B oracle). 7-step staged rollout: chart_profile builder -> formatter -> router (Stage 1 keywords + Stage 2 gloss) -> orchestrator `_VALID_DOMAINS` gate -> 3-layer test suite (Sulabh full pin: 11 windows, tier/favorable_count/warnings sequences ratified, Janma Tara band independently corroborated by the S24 Vishakha occupancy scan) -> golden rows `sulabh_muhurta_q1_stage1`/`q2_stage2` + dead `_KNOWN_GAPS["sulabh_dasha_q15"]` deletion -> baseline freeze. Ride-alongs closed en route: `_route_to_domain`'s hardcoded `current_dasha` fallthrough replaced with an explicit `else: raise ValueError` (closes the Session 58 carry-forward); `_GENERIC_REFUSAL_MESSAGE` topic list re-synced to 8 domains; `golden_harness.py`'s stale "3-domain"/"five-domain" whitelist docstrings fixed to six. `sulabh_dasha_q15` ("which month... best... major move") no longer hits the P2-order "not wired" lock and now resolves `MATCH_STAGE2`. Frozen comparison baseline: `diagnostics/golden_scorecard_20260711_195218.md` (`match=10/match_stage2=10/known_gap=1/new_gap=0`, 23 rows), supersedes `golden_scorecard_20260711_112836.md`.
- **T4 architecture** (Session 65) — AstroSage paragraph + palm reading are UPLOAD-TRIGGERED artifacts, never question-routed; calc_router / orchestrator / _VALID_DOMAINS / golden baseline untouched by all T4 work (sulabh_marriage_q10 KNOWN_GAP intentionally NOT closed). AstroSage paragraph is terminal-bare: displayed verbatim from parser output, no RAG, no LLM synthesis (S23 Path (c) verdict extends to it). RAG (Cheiro book-filtered) attaches to palm generation ONLY.
- **T4 golden semantics** (Session 65) — three rings: (1) deterministic envelope golden-tested as usual (extraction fixtures, prompt assembly, pure-Python output validators: disclaimer present / jargon blacklist absent / no dates-positions unsupported by provided context); (2) stubbed-LLM tests + harness rows (Stage 2 conftest-stub precedent — CI never asserts live prose); (3) human-rubric ratification artifact (S23-hardened rubric: citation-CONTENT accuracy, voice, no silent clause-dropping) IS this layer's frozen baseline.
- **T4 V1 boundaries** (Session 65) — astrologer.ask() QUARANTINED: frontend must not call it (conversational LLM Q&A stays OUT per S23); module retained for V1.1 Path (a) research only; app.py question path rewires to orchestrator.answer_question(). V1 palm reading = palm descriptions + Cheiro RAG only, one-shot, NEW module agent/interpretive/palm_reading.py (own system prompt; reuses DISCLAIMER + language/strict-context rules; no CQ/introduce/history); kundali×palm cross-verification deferred V1.1, gated on a lock-compliant deterministic chart-summary export (Parashara dissent logged: real capability cut). AstroSage display filter: Pratyantar + Lal Kitab sections extracted (parser UNCHANGED) but withheld at display layer.
- **Palm human checkpoint** (Session 65, extended S66 F1) — ALL vision-derived descriptions entering reading generation (palm_left, palm_right, hand_detail) must be displayed and USER-CONFIRMED first; app.py's programmatic auto-confirm (palm_*_confirmed = True on describe success) is an AI-reviewing-AI violation and was removed in the app.py rewire step. Extension provenance: S66 Ring 3 pass-1 (`diagnostics/ring3_palm_rubric_S66.md`) found hand_detail entering generation with no display/confirmation at all — closed by giving it the same review/confirm/discard checkpoint as the palms (S66 F1).
- **Per-feature retrieval + support gate + exemplar-echo guard** (Session 67, `palm_reading.py`, `8c1b8ab`/`b6dee9b`/`ffd504f`) — 10-feature registry replaces the old single whole-description RAG query; deterministic needle+0.30-floor support gate drives a Python-owned decline block for unsupported features (LLM banned from discussing them, Ring 1-enforced); exemplar sentences rewritten voice-only (zero interpretive content, user-confirmed wording) with a deterministic 6-gram echo guard. Full mechanics/justification: SESSION_LOG.md S67.
- **hand_detail RAG-exclusion LIFTED** (Session 67, supersedes the S66-era code-level exclusion — that behavior was never a formal lock here) — hand_detail's confirmed structured fields are now first-class features in the per-feature retrieval registry above; rationale (unreviewed AI output driving retrieval) no longer holds since hand_detail has carried a human checkpoint since F1.
- **V2 decision gate** (Session 68) — AstroSage PDF stays V1 (audit `diagnostics/astrosage_coverage_audit_S68.md`: 13 NOT-COVERED, 10 COVERED-PARTIAL sections). V2 interpretive template layer scoped to COVERED-PARTIAL sections first, classical-corpus-sourced, deterministic placement-keyed, no serve-time LLM; design pends F-C's grounding discipline landing first.
- **F-C: claim-level grounding = the A1 chunk-anchor mechanism** (Session 68 close-out) — the design question F-C was opened to answer ("what does claim-level grounding even mean for this generator?") is resolved as: the tagging contract + strip layer + Ring 1 V-1/V-2 validators ARE the grounding mechanism, not a stricter STRICT SCOPE wording or a claim-type drop list. A1 landed across 4 source commits + 1 test-alignment commit; suite 3200/3 → 3213/3.
- **Ring 3 pass-4 rubric additions** (Session 68) — two rows, consolidated: (1) anchor-fidelity spot-check (F-C) — human-sampled claim → cited-chunk faithfulness, scored against the SAME `reading_text_tagged` F5 now captures; denominator sourced from a reconstruction probe script (not the F5 capture's `valid_chunk_ids_count`, captured as `"unavailable"` — see the A1 accepted-gap register below). (2) warnings-block-P4-clean rule (F-A) — a `ValidationReport.warnings`-bearing run cannot score P4 clean; coverage warnings are pass-4 scoring evidence, not noise (see the F-A locked decisions below).
- **Query-template variant-iv rejected** (Session 68, F-C probe evidence) — `diagnostics/fc_retrieval_probe_S68.md` measured variant-iv (pure-Python clause-assembly query) degrading BOTH probed features' target-chunk ranks vs. the production variant-iii template (fingers: rank 1 → 3; life line: ranked 2/5 → not in top 10) — production `_build_feature_query`/n=3 retained unchanged, no code touched. Same evidence file doubles as F-D's LR-vs-LRH source-scope evidence (F-D stays queued, unblocked, undebated — see Carry-Forward).
- **F-A: supported-feature coverage check COMPLETE** (Session 68 close-out) — `_check_feature_coverage` (`palm_reading.py`) flags a supported feature never cited in the tagged draft: pure chunk_id anchor-intersection detection against `gated_results` (no keyword matching on prose, no thresholds), landmark-exclusion enforced BY CONSTRUCTION (`[OBS]` tags contribute nothing to the cited set, so a prose mention alone — e.g. "curves around the base of the thumb" — never counts as addressing that feature). Wired retry-feed (a coverage-only miss feeds the SAME single F2c retry as a Ring 1 failure, same 2-call hard cap, same `retry_used` flag — no new mechanism) + fail-open (a final-draft miss never enters `ValidationReport.failures`, never flips `passed`, never blocks display — it lands in the new additive `ValidationReport.warnings` field). Two-commit sequence (`d54c968` implementation, `afe6b4f` test alignment — 1 pre-existing test broke on the new coverage-only retry, fixed with a dedicated stub rather than perturbing the shared `_CLEAN_STUB_TEXT`), single push, main never red remotely. Suite 3213/3 → 3220/3 (7 new tests, 0 regressions).
- **Coverage computed on the FINAL draft** (Session 68 close-out, F-A) — `_check_feature_coverage` always re-runs against whichever draft actually ships (the first draft if clean, the retry draft if a retry fired), never the first draft's stale result; a feature is "addressed" only by a real `[<chunk_id>]` anchor citation belonging to that feature's own gated set.
- **F-B: absence-phrase regex broadening COMPLETE** (Session 68 close-out, `d01401a`) — `_is_absence()` (`palm_reading.py`) is now two-tier. TIER 1 (`_ABSENCE_PHRASES`) is the OLD 6-phrase fixed-substring list, byte-identical behavior, just recompiled as `re.escape`d case-insensitive regex. TIER 2 (`_ABSENCE_PATTERNS_BY_FEATURE`, new) adds per-feature `no <0-3 filler words> <noun> <0-6 filler words> visible` patterns, sourced from `_SUPPORT_NEEDLES` as the SAME single source of truth for each feature's noun (not a separately-maintained list). Conservative by construction, verified against real production text, not just asserted: the LIFE/HEAD/HEART LINE fields' real "no breaks, chains, forks, or islands visible" phrasing contains "islands" — a literal `markings/other features` needle — yet correctly stays unmatched for all three line features, because per-feature noun anchoring requires EACH feature's own noun ("life"/"head"/"heart"), none of which appear in that clause. Fixes the Pass 3 Findings #1 word-order gap (LEFT's "No marks clearly visible" vs. RIGHT's "No clear marks..." — same genuine absence, inconsistently classified by the old fixed-order phrase). Suite 3220/3, zero test breakage — the existing test suite's MARKS fixtures already used the OLD-list-matching word order, so no test exercised this gap; a single commit sufficed (no test-alignment commit needed).
- **Pass-4 comparability note — CORRECTED (Session 68 pass-4 close-out, F-E opened)** — the F-B close-out's original claim that `markings/other features` "SHRINKS vs pass 3 (F-B by-design)" was a PREDICTION against the 3 pass-3 reference runs' exact captured phrasing, locked into this file as if it were a general fact — a measure-first violation on the design-chat side (not F-B's implementation, which was correctly measured against what it was actually tested on). Live pass-4 data (3 fresh 2026-07-19 dogfood runs, `diagnostics/ring3_evidence_S68_pass4.md`) falsified the general claim directly: LEFT's vision-model MARKS phrasing this pass was `"No crosses, stars, grilles, squares, or moles clearly visible"` — a comma-separated list that defeats `_ABSENCE_PATTERNS_BY_FEATURE`'s `(?:\s+\w+){0,3}` filler-group regex (a comma is not `\s`), so the feature stayed `supported` via near-floor junk retrieval in all 3 pass-4 runs instead of shrinking. Corrected statement: whether `markings/other features` shrinks to genuine-negative-absence is CONDITIONAL on absence-phrasing shape, not a general guarantee — true for pass-3's phrasing, false for pass-4's. See F-E (S69 queue) for the fix candidate (comma-tolerant filler pattern). Incident logged, not silently amended.
- **Evidence-surface correction** (Session 68 close-out, F-B) — `.claude/read_prompt.md` is a MANUAL-PASTE SCRATCH SURFACE, not a durable evidence store: its content gets overwritten by whatever the user pastes next for an unrelated task (confirmed: `2bb2e44 Update .claude/read_prompt.md content` overwrote the 2026-07-18 palm RUN blocks with an unrelated Sade Sati question). Durable F5-captured RUN-block evidence lives in `diagnostics/dogfood_capture.md` (`frontend/app.py`'s `_capture_dogfood_run()` writes there, append-only) — see the new "Dogfood/diagnostic evidence surfaces" note under Reference Materials below. Future prompts citing RUN-block evidence should point at `dogfood_capture.md`, not `read_prompt.md`.
- **F-H LANDED** (S69, P1-P5b — commit sequence + probe detail in SESSION_LOG.md) — two-stage extract-then-voice (`claim_extraction.py` Stage 1 + `claim_voicing.py` Stage 2) replaces `palm_reading.py`'s single-call generation. Probe (`fh_stage1_probe_S69.md`): SC-1/2/3/5 PASS all 12 cells; model=gpt-4o-mini/temp=0 LOCKED for Stage 1 (no quality tradeoff vs. gpt-4o on extraction, measured). SC-4 FAIL all 12 (fate-line rises-from-life-line precondition never confirmed — "barely visible" states nothing about origin) is RULED a criterion mis-spec, not an extraction defect: the extractor's own conditional-decline (empty claims / E-4 exclusion) is the CORRECT accepted behavior this finding predicted, not a gap to force past. `_PARAPHRASE_OVERLAP_FLOOR=0.40` RATIFIED against the probe's pooled distribution (min=0.50, p25-p75=1.00) — CAVEAT: one-sided band only, no negative-control (fabricated-claim) distribution exists here unlike the 0.30 support-floor's two-sided justification; revisit at pass-5 or a dedicated overlap probe. E-4 (extractor-side conditional fail-closed) and V-5 (coarse `[FLOW]`/`[OBS]` doctrine-guard needle check, Ring-3-backstopped) are ACCEPTED, same disposition as the A1 gap register below (**S70 F-G4 update, `f94cecb`**: the `[OBS]` half of V-5 is RETIRED, not merely re-scoped — 4 consecutive 2026-07-22 dogfood runs showed it hard-failing on legitimate feature-naming `[OBS]` restatements, e.g. "The sun line is clearly present on your palm."; V-5 now covers `[FLOW]` only — see the T4 RATIFIED-LIVE entry below). Gaps (a)/(f) RETIRED BY CONSTRUCTION — E-1 (per-feature legality, `claim_extraction.py`) + V-3 (native tag legality, `claim_voicing.py`) replace the mechanism they were gaps IN, not just patch around them (register updated below). `_CORRECTIVE_CAP=1` is a voice/UX judgment call, not measured — Ring 3 is the revisit trigger. `_VOICE_MODEL="gpt-4o"` is UNTESTED (the probe measured Stage-1 extraction only) — a dedicated Stage-2 voice probe is a future candidate. Empty-retrieval behavior change RATIFIED: zero gated chunks -> zero LLM calls anywhere, decline-block-plus-disclaimer only (`_LOW_CONFIDENCE_ADDENDUM`'s old "still makes 1 call" path retired, no generic free-composed fallback). **F-G residual RESOLVED (S70 F-G1 `eb2c891` / F-G2 `34c7e6f` / F-G3 `4b6d15a` / F-G4 `f94cecb`)**: display checks (jargon/self-help/dates/length/banned-mention/exemplar-echo) are now fed INTO Stage 2's own retry loop via an `extra_validators` injection seam (F-G1) wired in `palm_reading.py` (F-G2); the verbatim exemplar sentences that were a standing echo-gravity source were deleted from the voice prompt entirely, not just guarded against (F-G3); V-5's `[OBS]` false-positive was carved out after it was found hard-blocking live dogfood runs (F-G4) — see the T4 RATIFIED-LIVE entry below for the pass-5 result this sequence unblocked. Dead code, deletion deferred to its own future prompt: V-1/V-2 validators, `_run_ring1_checks`, `_check_feature_coverage`, `_assemble_retrieved_passages`, `strip_generation_tags`'s old-vocabulary path, 4 skipped tests in `test_palm_reading.py`. Suite progression 3220/3 -> 3252/3 -> 3249/0/7 (0 regressions across P1-P5b, 2 self-caught corrections en route). Full arc: SESSION_LOG.md S69.
- **T4 RATIFICATION REVERSED (S71)** (2026-07-24) — **REVERSAL, reversing the S70 RATIFIED-LIVE call preserved below.** First row-level human scoring pass-5 ever received (design-chat adjudication, this session — the S70 close-out's "4/4" line below was never backed by a row-level rubric artifact; none existed until now) found Run A row 5 ("This line speaks to your mental faculties, indicating intellectual strength and the quality of your talents." `[C3]`, chunk `cheiroslanguageo00chei_1_p145_c0`) scores **U → FAIL**: `p145_c0` is HEAD LINE naming/taxonomy text ("intellectual strength OR weakness") that pass-3 Run A row 8 and pass-4 Run A row 4 already ruled cannot license a one-sided positive claim on this IDENTICAL chunk — pass-5's voiced sentence commits the identical error a third time. Stage-1's `valence="supports"` label on claims_inventory row C3 was weighed as a possible structural distinguisher (a two-stage extraction pipeline judging the chunk independently, unlike pass-3/4's single-call architecture) and ruled NOT dispositive: the label directly contradicts its own `claim_text`, which preserves the same "strength or weakness" neutral disjunction — self-contradicting extraction output, not evidence of genuinely different support. Full Run A ledger, independently re-verified against `diagnostics/dogfood_capture.md` this session (not transcribed from any draft): 3 U-rows total (rows 3, 5, 10 — row 3 a SECOND, independent precondition-mismatch defect, `p139_c0`'s "sweeps far out into your hand" clause never confirmed in either hand's LIFE LINE field; row 10 a summary restating rows 3/5's unsupported claims, pass-4's own summary-row convention), 3 C-rows, 4 D/D-frame rows. Run A: P1=N, P2=Y, P3=Y, P4=Y (3/4 — below the 4/4 ratification bar). Per pass-4 precedent (a single run's P1 FAIL fails the pass regardless of the other runs), this is dispositive on Run A alone; Run B/C were deliberately NOT scored this session (see `diagnostics/ring3_palm_rubric_S70_pass5.md`). T4 status reverts from RATIFIED-LIVE to **OUT OF V1 SCOPE — palm reading dropped, S71** (superseded same-day from an interim SCORED NOT RATIFIED read once the Option Z scope lock below landed; the interim read is preserved here for the session's own chronology, not because it is still current). **N=5 post-ratification failure counter: RETIRED** — never went live; its own precondition (an actual ratification) did not survive row-level scoring. **S68/S69's pre-ratification pass cap is now MOOT, not "back in force"** — Ring 3 pass 6 will not run; palm reading is out of V1 scope (see the V1 PALM DROPPED entry below; the Carry-Forward "Ring 3 pass 6" item this text originally pointed to has been removed, resolution recorded in SESSION_LOG.md S71). No "architectural stability window" lock on `palm_reading.py`/`claim_extraction.py`/`claim_voicing.py` was ever recorded in this file to lift — noted here rather than fabricated: those modules were never under a documented freeze distinct from the standing commit-ratification-token rule (Working Style #14), which continues to apply as always. No production code touched this session (docs-only commit). Full adjudication (3 candidate rulings, ADJ-A/B/C) recorded in SESSION_LOG.md S71.

  *Original S70 ratification text, preserved verbatim (historical, superseded by the reversal above):*
  T4 RATIFIED-LIVE (Session 70, 2026-07-23) — Ring 3 pass 5 gate satisfied: P6a (`be64da6`, F5 capture schema — claims inventory + two-stage retry fields) + P6b (`27889c1`, two-mode Stage-1 checkpoint), F-E (`84c49f1`, comma-tolerant absence filler groups), and the F-G loop (F-G1 `eb2c891` extra-validator seam, F-G2 `34c7e6f` display-checks-feed-Stage-2-retry wiring, F-G3 `4b6d15a` descriptive voice guidance replacing verbatim exemplars, F-G4 `f94cecb` V-5 `[OBS]` carve-out + adjacent-tag prompt hardening) all landed before this pass. 3 fresh live dogfood runs 2026-07-23 (`diagnostics/dogfood_capture.md`: 18:42:14 Run A, 18:43:13 Run B, 18:44:11 Run C+HAND_DETAIL) scored 4/4 on the pass-4 rubric's P1-P4 rows — the SAME rubric pass 4 scored P1 FAIL on (S68), now clean 3/3; deterministic Ring 1 corroborates on all 3 (`passed=True`, `validation_failures=NONE` — each run's `stage2_first_attempt_failures` shows the retry catching a real first-draft doctrine_guard/jargon/self_help hit and clearing it, i.e. the F-G wiring visibly doing its job, not merely absent). T4 status upgrades directly from SCORED NOT RATIFIED (S68 pass 4) to RATIFIED-LIVE — Ring 3's own pass cap (5 total pre-ratification, S68/S69 lock) is satisfied on the FIFTH pass, the last attempt before a mandatory design-chat re-litigation would have fired. **N=5 post-ratification failure counter: STARTED at 0** — a NEW, separate counter from the pre-ratification pass cap, tracking any future live dogfood failure against the now-RATIFIED baseline. **Option S trigger**: NOT FIRED — pass-5's P1 did not fail, so the feature-scope-down fallback was never invoked; kept in the register, unchanged, as a POST-ratification-reopen contingency only (a future regression severe enough to threaten the ratified baseline, not a routine bug). Full-suite verification for this close-out: 3281 passed, 0 failed, 7 skipped (re-run directly, per Working Style #12 — the instructing prompt's own stated baseline of 3279/0/7 undercounted by 2, the two F-G4 tests added after that count was last taken; see SESSION_LOG.md S70).

- **V1 PALM DROPPED (S71, 2026-07-24)** — Option Z scope lock, docs-only, supersedes the T4 RATIFICATION REVERSED entry's interim "SCORED NOT RATIFIED" read above. RATIONALE: Palm reading fails V1's accuracy bar. S65-S71 arc: five Ring 3 pass ratifications attempted, S70's ratification reversed at S71 (row-5 p145_c0 defect + row-3 p139_c0 defect, both self-contradicting Stage-1 valence labels — see `c19ea0c`). Root cause is task-model fit: GPT-4o-mini cannot reliably judge classical-text valence at the accuracy the T4 surface requires, because classical palmistry prose is inherently hedged, disjunctive, and conditional. S71 diagnostic scan confirmed 25 candidate chunks in `cheiroslanguageo00chei_1` alone with the neutral/disjunctive shape that triggers the defect class, and explicitly flagged that `p139_c0`'s precondition-mismatch defect is a STRUCTURALLY DIFFERENT class the scan did not catch — future defect classes are likely. Additionally: the user's stated V1 objective (layman palm Q&A + palm/astro combo) is out of scope by two prior locks — LLM-generated interpretive Q&A excluded from V1 after path (c) failure, and kundali×palm cross-verification deferred to V1.1 at S65 (Parashara dissent). Moving palm interpretation from T4 upload-time to query-time would relocate the Stage-1 defect into an unscoped Q&A surface, not resolve it. DECISION: V1 palm-side user flow DROPPED — no palm upload UI in V1, no T4 palm reading generation, no palm citations in Q&A routing. All palm-side code STAYS in the codebase (`palm_processor.py`, `agent/interpretive/palm_reading.py`, `claim_extraction.py`, `claim_voicing.py`, Cheiro RAG chunks, Ring 3 harness, tests) — NOT deleted, NOT rewritten, preserved intact for V1.1. V1 ships T1/T2/T3 deterministic chart answers + AstroSage paragraph display only (chart-only user flow). This is a SCOPE decision, not a technical failure — the F-H two-stage architecture shipped and works; the model-fit limitation is upstream of the architecture (see SESSION_LOG.md S71 addendum). Feature-flag wiring to formally gate the palm UI path off is a SEPARATE follow-up prompt, not done here (docs-only lock, no production code touched). No production code, test, or config file edited by this entry's own commit.
- **Frontend surface (S72, 2026-07-24)** — Streamlit is the sole frontend surface through V1 AND V1.1; no new frontend surface (FastAPI, mobile client, external API) opens before V1.1 ships. Reason: every V1.1 backlog item (palm revisit, Yogini/Ashtottari dashas, D10/D7, kundali×palm cross-verification, Stage-1 parallelization) is backend work; abstracting a client API now would freeze a contract V1.1 will keep reshaping. Streamlit-specific commitments (session_state layout, widget-driven rerun flow) are NOT locked by this decision — a swap of the current frontend implementation stays open if V1.1 friction demands it. Post-V1.1 (FastAPI layer, mobile clients, deployment/hosting architecture) is explicitly out of scope for this decision and not scoped here.
- **Palm UI gate mechanism (S72, 2026-07-24)** — Option Z (S71) is enforced in `frontend/app.py` via module-level constant `_PALM_ENABLED = os.environ.get("ASTRO_PALM_ENABLED", "0") == "1"`. Palm UI render blocks are wrapped with `if _PALM_ENABLED:`; palm session-state init, palm imports, and all palm downstream calls (which live inside the gated render blocks) are unchanged. `orchestrator.answer_question()` receives no palm state (recon verified) and is not gated. V1.1 re-enable is a config flip (set `ASTRO_PALM_ENABLED=1`), not a code diff.
- **Palm UI gate mechanism — PLANNED, NOT YET IMPLEMENTED (S72, 2026-07-24)** — spec for the S71 V1 palm-UI drop's (Option Z) eventual enforcement mechanism, recorded here as a design lock ahead of implementation; the S71 entry above already flags this wiring as a deferred follow-up prompt, not done as of S72 either (docs-only session, no `.py` file touched). Planned shape: an environment variable `ASTRO_PALM_ENABLED` (default `"0"`, palm UI hidden) gates only the `frontend/app.py` UI entry points that expose palm to end users; all palm-side code paths (`palm_processor`, `agent/interpretive/palm_reading.py`, Cheiro RAG, Ring 3 harness, tests) stay imported and testable regardless of the flag. Intent: V1.1 re-enable becomes a config flip, not a code diff. Design requirement for whichever future prompt implements this: the gate must be checked at UI-render time (not import time), so tests can toggle it via monkeypatch without module reloads.
- **V1.5 Rule Engine Foundation phase EXISTS, gated on 4 prerequisites (proposed 2026-07-25, UNRATIFIED — no corroborating design-chat session found; not yet run through the 6-agent pass)** — generalizes the domain-assembler pattern before V2 scales 10x. Gate: (1) V1 shipped incl. palm UI gate, (2) Ashtottari wired, (3) D10+D7 built, (4) palm revisit decided. Design: `playbook_export/decisions/rule-engine-v1_5-design.md` (two-tier YAML/Python engine, ordinal confidence from 4 structured signals — no fabricated numerics, ChartContext v2 + signal-axis validator, rule-of-three schema derivation). Premature start = path (c) failure mode class. Scope refinement (2026-08-01, additive, gates unchanged): see that design doc's "Addendum 2026-08-01" — compound conditional doctrine is genuinely cross-chapter, a future extraction pass needs a cross-chapter linking step. Cheiro pilot (V1.1) is the validation gate for this approach before astrology-book application — see the V1.1 register's offline-verified-extraction pilot entry above for the reverse cross-link. GATES UNCHANGED BY THE PILOT: "not before V1 ships" and the rule-of-three (4th hand-coded domain, Ashtottari dasha, before generalizing) still apply to the GENERALIZED multi-domain assembler described in this entry — a single-domain Cheiro/palm pilot running is not the gate being satisfied; do not conflate "pilot running" with "V1.5 gates lifted."

Older superseded/shipped-feature decisions (Sessions 15-25) archived to SESSION_LOG.md's compression section — not needed per-query once a module ships and its convention lives in the code/tests themselves.

- **PALM RETRIEVAL: PAGE-RANGE FILTER (S81)** — palm searched all 463 Cheiro chunks per feature; adjacent-rank score gaps are 0.016-0.034 with no cliff, so out-of-chapter near-misses filled the gate of 3. Fix is a chapter-range pre-filter, map at `data/cheiro_feature_pages.json` (52 headings extracted from the page-level export; authoritative — earlier versions omitted 6 chapters and had 4 wrong boundaries). Validated: head and fate top-3 are all in-chapter, human-verified correct doctrine. NOT A THRESHOLD PROBLEM — the sweep gave min_n_all = fate 15, head NEVER, heart NEVER, so no value of n fixes it.
- **ONE-CALL-PER-FEATURE CONTRACT (discovered S81, treat as locked)** — palm retrieval must make exactly ONE `search()` call per observed feature at `_N_RESULTS_PER_FEATURE`. Asserted independently by `test_query_template_two_hand_merged_quality_literal_shape`, `test_search_filters_to_canonical_cheiro_book`, and `test_one_feature_search_failure_does_not_kill_reading_other_feature_succeeds`. These encode design intent and MUST NOT be edited to accommodate a change. Any filter must therefore act inside the query (Chroma `where`), not as a widen-then-post-filter.
- **CHEIRO CORPUS EXONERATED FOR PALM (S81)** — 463 chunks, all named target chunks exist by direct id lookup, zero empty text pages in p133-182. Path D is NOT required for palm. Cheiro remains the only book with a labelled eval set (Ring 3), so it stays the pilot for retrieval work.
- **F4 CENSUS VOID (S81)** — `data/progress/*.json` covers 14 of 22 books (5,219 of 7,357 pages) and has NO `native_text_present` field. The 75.9% figure is unusable and the pre-registered cut did not fire. F4 is UNMEASURED, not disproven. Corpus-wide sizing must read PDF page counts directly, never progress.json.
- **EXISTENCE vs RANK are different questions (S81)** — "absent from top N search results" is not "missing from the corpus". Establish existence by direct id lookup (`collection.get(ids=[...])`), never by whether a chunk appears in search output. Conflating these produced a false DATA verdict and three sessions of wrong direction.
- **HASTA SAMUDRIKA unusable, doctrinal gap real (S81)** — 449 pages, 4 with >=50 alphabetic words, zero Devanagari; typical page is OCR noise. Do not ingest or re-ingest it as-is. The gap it leaves is genuine: palm grounds on Cheiro (Western) with the Indian tradition absent. Fix path is re-sourcing an English edition or Devanagari OCR + translate, not repair.

- **Palm page-range gate ON (S82)** — `_FEATURE_PAGE_FILTER_ENABLED = True`. Each palm feature makes exactly ONE `search()` call carrying `page_ref=(start, end)` from `data/cheiro_feature_pages.json`, enforced as two `$and` clauses in the Chroma where clause. A range matching nothing yields an empty list for that feature and routes to the decline block — the unfiltered fallback is GONE by design. Evidence: `diagnostics/onoff_range_gate_S82.md` (0334038), `diagnostics/census_feature_page_ranges_S82.md` (a69549a). Deeper unfiltered retrieval was measured and REJECTED: chapter provenance is metadata and is not recoverable from chunk text, so an LLM given a wider window cannot distinguish another line's doctrine from the target feature's. Reversion: set the flag False for a byte-identical pre-S81 path, no other change. Scope: palm-feature retrieval, Cheiro only.
- **5c steps 4-5 COMPLETE — all relational rules live (S93)** — HEAD `67c5cc7`, `wip/interpretive-pilot`, pushed. parked=[H_022] only; validated=47; retired=[H_001].
  - H_017 (distant+Line of Life) + H_016 (fixed contradictory -> medium+Line of Life) un-parked, deferred-live (no medium/distant hand). H_014 un-parked as single Branching value:null+Line of Heart ("upward" dropped); BRANCHES_TO fully wired (vision emits + extractor maps, Line of Heart in menu) so H_014 is LIVE-capable — only a branching-hand sample missing.
  - KNOWN DEBT: multi-landmark BRANCHES_TO dropped whole; str->set fix is multi-layer/Opus, DEFERRED (no branching hand exists).
  - NEXT = 6a (OPUS): H_022 capture design — head TERMINATION menu +"Line of Heart" (p154) + Position:high VALUE axis (TERMINATION maps to Position as LANDMARK; high is a separate value/target split). Then 6b vision edit+re-dogfood, 6c author H_022 (Sonnet). Housekeeping: gitignore scratch files. Detail: SESSION_LOG.md S93.
- **Relational-target machinery LIVE + 5c CORE COMPLETE (S92)** — HEAD `e400de2`, `wip/interpretive-pilot`, pushed. P-degree pipeline live end-to-end; H_027 first live relational rule.
  - Relational-target machinery LIVE + suite-green (3505 passed). Vision emits inline per-line ORIGIN/TERMINATION/PROXIMITY/BRANCHES (closed menus, Mars split, Junction, LAW 2). `extract_relational_targets` parses inline + legacy formats. RELATIONAL-block drop fixed (was ~40%).
  - `value:null` = target-only wildcard antecedent. CORRECTION (S92): this required an ENGINE CHANGE (2.5, `e2297ab`) — the original `_antecedent_fires` silently failed a directed antecedent when an observation value was present; the value-check is now gated on `value is not None`. 'touching' is P-EXCLUSIVE (absent from global value pool by design; only extract_proximity_observations supplies it — do NOT add it to the pool). Retirement convention: move a rule to the top-level `retired_superseded` array (loader reads validated_candidates only).
  - 5c-P: proximity degree captured (`extract_proximity_observations`), INERT — not wired to match, not bound.
  - DONE (S92): P wire+flatten, bind, H_027 migrate + H_001 retire, S89-comment retire. NEXT = 5c steps 4-6: (4) un-park H_016 (fix contradictory -> Proximity=medium+Line of Life) / H_017 (distant+Line of Life) — authorable now, LIVE head->life medium/distant validation deferred (no separated-origin hand; RATIFIED head->life=touching is genuine, not bias); (5) H_014 (Branching:null+Line of Heart, drop "upward") — PREREQ: verify BRANCHES_TO -> targets; (6) H_022 BLOCKED on Line-of-Heart TERMINATION menu ext (p154) + Position:high capture. Re-verify each vs source_quote; RATIFIED authorizes commits. Full detail: SESSION_LOG.md S92.

## Windows Paths (hardcoded)
- Tesseract: `C:\Program Files\Tesseract-OCR\tesseract.exe`
- Poppler: `C:\Program Files\poppler-26.02.0\Library\bin`

## Module Order
```
pdf_processor → image_extractor → chunker → translator → embedder → ChromaDB
query_engine + chart_calculator → astrologer → session_manager
```

**SESSION_LOG split (S81)** — `SESSION_LOG.md` holds head-matter + S67 onward; Sessions 19-66 are in `SESSION_LOG_ARCHIVE_S19-S66.md`, verbatim. Any citation above to `SESSION_LOG.md S<n>` with n<67 resolves to the archive. Boundary derived from the lowest body session still cited here (S67) — SCOPE GUARD: never split above a live citation; the file is not chronologically ordered and 34 sessions lack headers, so boundaries are byte offsets of exact-matched header strings, never session arithmetic. TUNING NOTE: re-derive by re-grepping this file's `SESSION_LOG.md S<n>` citations; re-split past ~150 KB live.

## Reference Files (load only when relevant)
| File | Load when |
|---|---|
| .claude/architect.md | new file, schema, pipeline change |
| .claude/business.md  | new file, user-facing change |
| .claude/critic.md    | new file, any code change |
| .claude/qa.md        | new file, any code change |
| .claude/ui_ux.md     | any frontend or UX change |
| .claude/debate.md    | agents conflict, multiple valid options |
| diagnostics/KNOWN_PATTERNS.md | before starting any diagnosis (check first) and before closing any diagnostic finding (add a row) |

## Working Style (non-negotiable)
1. **REVIEW before PROCEED** — flag at least one issue before approving any edit
2. **SAMPLE before SCALE** — propose sample validation before full dataset runs
3. **HARDEST CASE first** — test on edge cases, not simple ones
4. **THRESHOLD DISCIPLINE** — every numeric threshold needs justification + scope guard + tuning note
5. **AI reviewing AI** — flag when output has no human review; never chain AI decisions without human checkpoint
6. **SURGICAL EDITS** — no full file rewrites; Python 3.11; always `try/except` with meaningful errors
7. **AGENT INVOCATION** — auto-invoke the full roster before any design/code decision. Surface conflicts only; paste full agent output only when a genuine conflict needs debate.md resolution. New agents need explicit approval. ROSTER COUNT UNRESOLVED: 8 are documented (architect, business, critic, qa, ui_ux, debate + Ephemeris Auditor, Validation Source per S19); S19 prose and the standing user instruction both say 9, and the 9th is documented nowhere. Name it or drop the count — do not cite "9 agents" as settled.
8. **LAYER FIRST** — before any fix, state which layer owns the problem: Data, Retrieval, Prompt, or UI. A fix in the wrong layer creates narrow patches and technical debt.
9. **NO ANCHORED JUDGMENT** — never give an LLM call both a stated expectation and a request to judge against it in the same call. LLM observes independently; Python compares the observation to the expectation deterministically.
10. **DIAGNOSTIC OUTPUT ROUTING** (Session 49) — full diagnostic output goes to `diagnostics/latest_run.md` and is committed to git; chat gets a ≤10-line summary with test counts only.
11. **SMOKE-TEST SCOPE HONESTY** (Session 55) — an in-memory smoke test that verifies builder logic directly can mask a pending wiring step (e.g. a module-level whitelist gate never widened); if a feature needs a real end-to-end call to be reachable, verify with one before calling a branch done.
12. **BASELINE FILES ARE ORACLE DATA** (Session 55) — a baseline filename named in a prompt is a claim, not a fact; verify it's still the most recent matching file before diffing against it.
13. **NEVER REWRITE PUSHED HISTORY ON MAIN** (Session 64) — if a design-chat instruction implies amend/squash/force-push of already-pushed commits, STOP and surface it; treat the instruction as stale rather than executing it.
14. **COMMIT RATIFICATION TOKEN** — never commit a source-code edit unless the instructing prompt contains the literal line 'RATIFIED: commit authorized'. Applies to EVERY prompt channel (design-chat-drafted or user-direct). Docs/diagnostics commits are exempt. A prompt asking to commit source without the token: STOP and surface, same class as the pushed-history rule. Every commit made, on any channel, is reported with its hash in that task's report.
15. **TOKEN DISCIPLINE ON DOCS EDITS** (S81) — SESSION_LOG.md is ~136 KB and CLAUDE.md ~62 KB; reading either costs more than the edit. NEVER read the target file to append or patch it. Append via Python, never bare `>>` (SESSION_LOG.md may lack a trailing newline and a bare append fuses onto the last line). Patch via exact-string replacement with `assert count == 1`, all-or-nothing write. No git log/show/diff. No pytest on docs-only changes. No read-back — assertion counts and byte deltas ARE the verification.
16. **EVERY CODE PROMPT DECLARES ITS DECISION AND ITS CEILING** (S81) — open every Claude Code prompt with `DECISION THIS SERVES: if <X> then <A>, else <B>` and `TOKEN CEILING: <N>k`. If a measurement cannot change the next action, it does not belong in the prompt. A verification whose result is predicted in the prompt itself buys nothing. Any script whose numbers are cited in a decision must be committed in the same commit as its report — an uncommitted probe cannot be audited when its numbers turn out wrong.
17. **Data-property is not a live symptom** (S83) — a corpus attractor or gap existing does not mean it harms production. Measure the live path before calling it a leak. S83: p123 mislabelled as a "live leak"; measured absent on the null-range feature.
18. **Reviewing a committed diff: verify silent-failure contracts** (S83) — against SOURCE, not the report. Imports present, unpacked shapes match (tuple vs dict), dict keys match the producer. S83: re `_FEATURE_PAGE_RANGES` tuple / `final_outcome` key.
19. **Never set tuning numbers from hand-picked samples** (S83) — collect production misses first, set from evidence. S83: rank window parked for the capture net.
20. **Failure-capture discipline: log threshold-free** (S83) — log failures only, categorical + threshold-free; humans decide fixes at monthly review; add a near-miss margin log only if silence rows prove insufficient.
21. **KNOWN-PATTERNS DISCIPLINE** (S84) — before starting any new diagnosis, grep `diagnostics/KNOWN_PATTERNS.md` first; a symptom already settled there does not need re-deriving, only re-verifying against its cited evidence. Before closing any diagnostic finding, add a row there — that file is the durable record, not a narrative `diagnostics/*.md` file (do not create a new one-off `diagnostics/<topic>_S<xx>.md` for a conclusion that belongs in the index; raw probe scripts/output stay where they are as evidence, only the settled conclusion gets a row).

## Varshaphal House-Counting Convention (Session 18)
`resolve_house_counting_lagna()` is the canonical house-counting reference for any Varshaphal-derived bhav calculation (prefers AstroSage parsed Lagna, year-matched; else computed + boundary flag); call it rather than reading Lagna directly off `varshaphal_data`.

## Palm Diagnostic Principles (2026-08-06)
Learnings from the head/life-line descriptor fixes (width, direction) and the extraction incompleteness-retry guard, this session.

1. **Silent-feature diagnostic ladder** — before calling a silent palm feature a "coverage gap", walk in order: (a) does `palm_processor.describe_palm_image`'s vision descriptor even ask for the dimension the rule keys on? (b) did the extractor produce that token, or drop it / file it under the wrong attribute? (c) does a rule key on that exact token/value? (d) is the silence actually CORRECT (no classical doctrine covers it)? Do not assume "coverage gap" before walking (a)->(d).
2. **Not all silence is a gap** — a plain healthy heart line or a medium-width life line has no Cheiro doctrine to fire on; forcing a rule there is unsourced invention. Honest silence is the product working as designed.
3. **Rules-vs-vision mismatch is the dominant palm failure class** (this arc) — rules keyed on narrow/direction/clear dimensions the vision descriptor never elicited. Fix = add the missing dimension to the descriptor, not the rule. Validated: adding width to the descriptor unblocked L_001.
4. **Instrumentation must be rendered, not just stored** — a diagnostics dict key (e.g. `phrase_promotions`) is invisible until the dogfood FORMATTER (`frontend/app.py`) actually renders it. Any new diagnostic field must be wired through to the capture, not just populated upstream.
5. **`attribute_value_binding` pattern** — constraining observation extraction = declare the legal value set per attribute in `ontology_registry.json`'s `attribute_value_binding`; `observation_extractor` consumes it generically. Never enumerate prose phrases in code or lexicons to map observations.
6. **Let borderline be** — for inherently ambiguous vision features (e.g. straight vs slight slope), do NOT engineer forced determinism or multi-sample stabilization. Accept the honest per-call read; honest variance beats confident-wrong.

## Calculation Architecture (Session 19+)
Every new calculation module lives in its `calculations/` subpackage; never add calculation logic to a top-level file.

**Package structure:** `core/` (chart_d1, panchanga, aspects, dignity) · `vargas/` (D2-D60, vimshopaka) · `strength/` (shadbala, ishta_kashta, bhava_bala) · `dashas/` (vimshottari, yogini, chara, ashtottari, mudda) · `yogas/` (detector + catalog/) · `transits/` (gochara, sade_sati, chandrabala, transit_aspects) · `ashtakavarga/` (single module: compute_bav + compute_sav; bav.py/sav.py stubs superseded and removed, Session 54) · `jaimini/` (karakas, arudha, padas, strength — stronger co-lord cascade Ch.15 §15.5.1, rasi_aspects — rasi drishti Ch.10 §10.3) · `annual/` (varshaphal, muntha, sahams) · `helpers/` (house_counting, ephemeris)
**Canonical helpers:** `resolve_house_counting_lagna()` (`helpers/house_counting.py`) — canonical for ANY Varshaphal-derived bhav calc; `helpers/ephemeris.py` (`sidereal_longitude`/`sidereal_position`/`EphemerisError`, Session 52) — canonical for sidereal-standard `swe.calc_ut()` calls; see Locked Decisions for the 3 direct-by-design exceptions.
**Validation protocol** (per module, on top of Working Style #2/#3): empirical validation across 4 reference charts before locking a formula; zero free parameters (test alternative hypotheses, rule them out); AstroSage parity where applicable, JHora oracle where not; document irreducible cross-software noise as discovered.

## Reference Materials
**Calculation specifications:** `data/pdfs/Vedic Astrology_ PVR Narashimha Rao.pdf` — primary reference for P1-P6 (path corrected Session 57; the previously-documented `project_files/classical_references/...` path does not exist in this repo); PVR authored both this book and JHora (book = formulas/justification, JHora = numerical ground truth); both consulted before implementing any new calculation module. `data/pdfs/[Deepak Kapoor] Astronomy and Mathematical Astrology_text.pdf` (Session 75) — Kapoor Institute of Astrology, Bharatiya Vidya Bhavan, mathematical exposition; PVR (BPHS-tradition) and Kapoor together are the primary spec sources — Kapoor for Vimshottari math (Ch IX), Shadbala/Bhava Bala (Ch XVI-XIX), and the ayanamsa 1900+ table (Ch IV); PVR remains authoritative for interpretive doctrine and yoga detection.
**Validation oracles:** AstroSage PDFs (4 reference charts) for secondary parity; JHora exports as primary parity where AstroSage doesn't expose the calculation. Sheridan (1984-05-27 08:00, Durban SA) and David (1976-01-19 22:00, London UK) are fully activated — birth data extracted from their AstroSage PDFs; geocoded in `tests/fixtures/geocoded_locations.json`.
**Interpretive RAG (separate, do not pollute):** ChromaDB ~7,281 chunks across 14 classical texts, for Tier 4 interpretive answers in Parashara's voice. Modern textbooks (incl. PVR's) are deliberately excluded from RAG to preserve classical voice and avoid single-author bias.
**Dogfood/diagnostic evidence surfaces** (Session 68 F-B close-out): `diagnostics/dogfood_capture.md` is the DURABLE, append-only F5 capture log (`frontend/app.py`'s `_capture_dogfood_run()`) — this is where real `## RUN <timestamp>` blocks (confirmed LEFT/RIGHT/HAND_DETAIL descriptions, reading text, sources, validation) actually live and persist across sessions. `.claude/read_prompt.md` is a MANUAL-PASTE SCRATCH SURFACE ONLY — the user pastes ad-hoc instructions there per-task, and its content is routinely overwritten by the NEXT unrelated paste (confirmed: it once held the 2026-07-18 palm RUN blocks, since overwritten by an unrelated Sade Sati question, commit `2bb2e44`). Cite `dogfood_capture.md` for any RUN-block evidence; never assume `read_prompt.md` still holds what a past session wrote there.
## Diagnostics & Reporting Conventions
- `diagnostics/latest_run.md` is OVERWRITE-ONLY per Code run. Every Code prompt starts by truncating this file and writing only the current run's output. Never append. Historical output belongs in commit-anchored diagnostics (`diagnostics/<topic>_S<xx>.md`), not accumulated in `latest_run.md`.
- `.claude/read_prompt.md` is the manual paste surface for design-chat review. Same overwrite rule.
- Do not conflate the two files in task prompts.
- Every session that receives new external data (Drik, AstroSage, JHora, Prokerala, book citations) MUST append to `docs/PROJECT_FACTS.md` in the session-close commit per its §6 append protocol. No entry accepted unless traceable to a committed file.

## Known Source Divergences / Accepted Gaps (V1) — locked V1 (Shadbala/Bhava Bala divergences; A1 chunk-anchor accepted gaps below)
- **Shadbala Drik Bala** — REAL since Session 46. 28/28 JHora v8 parity (4 charts, ±0.5 Virupa). Formula: BPHS Ch.28 Sphuta Drishti kernel + B.V. Raman Art.109-120 aggregation (Pinda/4) + smooth-taper corrections at 3 BPHS segment boundaries (documented in drik_bala.py docstring). Moon classification: benefic when 90<=elongation<270 (Ashtami-to-Ashtami), NOT paksha; Mercury: same-rasi association count, Moon included. OPEN: Mars 180-210 plateau (S=60) continuity-derived, zero data coverage across all 4 charts — revisit trigger: any chart with a Mars aspect landing in that arc. AstroSage parity NOT expected on this component (JHora-vs-AstroSage genuine divergence, e.g. Sulabh Saturn 17.46 vs 10.89); JHora primary.
- **Bhava Drishti Bala** — REAL since Session 53. Kernel = raw PyJHora `__bhava_drik_bala_calc_1` piecewise with ADD-ON specials (Saturn/Mars/Jupiter) — intentionally DIFFERENT from drik_bala.py's smooth-taper graha kernel; both independently oracle-validated; do NOT unify them. Quarter rule (all planets ×0.25 except Mercury/Jupiter, which get full value), signed sum (no /4), no drishti-house gating, Sripati/Porphyry Bhava Madhya, classification reuses drik_bala.py's dynamic Session 46 rules (a fixed benefic list was empirically ruled out — Sulabh/David Mercury flips malefic). PyJHora's own aggregation NOT ported (row/col indexing bug + fixed benefic list). Oracle = AstroSage BhavBala table, 48/48 houses ±0.5 (measured max 0.15); JHora bhava-level parity unchecked. combustion.py-style exception re-wrap does NOT apply here (bhava_bala.py has no local EphemerisError to preserve).
- **Chesta Bala cross-chart divergence** — Sheridan/David show wider kala_total/chesta spread than Surbhi's documented divergence (same elongation-formula root cause); AstroSage parity tests scoped to Sulabh+Surbhi only. Fix: revisit only if a future phase needs Sheridan/David precision.
- **Planet key casing** — shadbala_totals.py keys are lowercase (`"sun"`); bhava_bala.py/SIGN_LORDS use Title-case (`"Sun"`). `chart_profile.py`'s `shadbala_titlecase` bridge is the single conversion point — do not add ad-hoc `.capitalize()` calls elsewhere.
- **Kala Bala Sun cross-chart (Surbhi)** — `uncertainty_virupa` general = 2.0 (AstroSage-parity basis), vs. Surbhi's ±59 Virupa override (Jupiter/Saturn Abda/Masa divergence) — unchanged, chart-specific, takes precedence. Full detail archived in SESSION_LOG.md.
- **Pancha Mahapurusha real-chart validation** — 3/4 charts unvalidated (SESSION_LOG Session 40).
- **Combustion orbs** (Session 51) — PVR book SILENT (p.114 qualitative only, no degrees, no retro rule) — spec falls to Surya Siddhanta convention (Mo12/Ma17/Me14[12R]/Ju11/Ve10[8R]/Sa15). PyJHora const.py:608-609 diverges (Ju/Ve swapped: 10/11; non-classical retro Ma8/Sa16) — we follow classical; outcome-sensitive only 10-11deg from Sun. AstroSage exposes NO combustion surface: Deeptadi avastha is dignity-only, never assigns Vikala (verified Surbhi p.23, Mercury=Muditha at 3.6deg). Oracle = hand-falsified arithmetic on AstroSage p.3 longitudes. No deep/casual sub-threshold in V1 (no classical source quantifies one).
- **A1 chunk-anchor accepted gaps** (Session 68 F-C close-out, gap (f) added by F-A close-out; all in `agent/interpretive/palm_reading.py` unless noted) — six gaps, each registered at a code-site comment + this module's own docstring + here (the 3-place convention): (a) V-2 anchor legality is union-only (`_check_anchor_legality`) — RATIFIED as a permanent accepted gap for the single-call architecture it applied to: a real gated chunk_id cited under the WRONG feature's sentence was not mechanically caught there; covered by the Ring 3 pass-4 human anchor-fidelity spot-check instead of a future validator. **RETIRED BY CONSTRUCTION (S69 F-H close-out)**: `claim_extraction.py`'s E-1 validator checks chunk_id legality PER FEATURE at extraction time, natively closing this class of gap for the current two-stage architecture — `_check_anchor_legality`/V-2 itself is now dead code (see `palm_reading.py`'s own module docstring), not deleted. (b) V-1 tag completeness is position-only (`_check_tag_completeness`) — an untagged sentence sandwiched BETWEEN two valid tags is uncaught; sentence-splitter improvisation is explicitly banned, so this stays open by design (also documented in `tests/interpretive/test_palm_reading.py`'s own comment). (c) Heart-line retrieval, NOT a corpus gap — REFRAMED S81. p157/p158 are Plate XVIII plus its blank verso, confirmed independently; across the entire doctrine range p133-182 zero text pages are empty and all 9 empty pages are page_type='diagram'. Doctrine is COMPLETE. The old "p160_c3/p159_c2/p160_c1 never rank" framing is OBSOLETE: those were an unvalidated S68 pick, and with the page filter the top 3 (p159_c3, p160_c2, p161_c0) is correct doctrine. Do not treat this as a corpus defect. Former text: (c) Heart-line corpus gap (`_retrieve_per_feature`, evidence `diagnostics/fc_heartline_corpus_S68.md`) — p.157-158 have zero chunks, `p159_c2`'s "first\nfinger" line-wrap defeats literal matching, and positive-configuration doctrine (`p159_c2`, `p160_c1`) exists but never ranks in embedding retrieval for this feature; non-harmful under A1 (falls back to `[OBS]`, never a fabricated citation). (d) `CHUNK_ANCHOR_TAG_PATTERN` couples to the current ingestion id schema (`*_p<n>_c<n>`) — a re-ingested corpus with a different id shape would silently break tagging; the V1.1 corpus re-ingestion item below must revisit this pattern in the SAME change. (e) `valid_chunk_ids_count` in the F5 dogfood capture is captured as `"unavailable"` (`frontend/app.py`) — not exposed on `PalmReadingResult`; pass-4's anchor-fidelity denominator instead comes from a reconstruction probe script, not this capture line. **RETIRED (S70 P6a, `be64da6`)**: the `valid_chunk_ids_count: unavailable` placeholder line is REMOVED from the F5 capture entirely, not merely fixed — P6a's claims-inventory capture (`claim_id | feature | chunk_id | valence | ...`) supersedes it as the evidence source; `test_capture_dogfood_run_no_longer_writes_valid_chunk_ids_count` asserts the old line's absence. (f) Coverage shared-chunk false-positive (`_check_feature_coverage`, F-A) — a chunk_id gated under TWO features marks BOTH addressed when cited once, regardless of which feature the citing sentence is actually about; a direct consequence of gap (a)'s union-only V-2 anchor semantics (no sentence → feature attribution exists to disambiguate). RATIFIED as a permanent accepted gap, same disposition as (a): backstopped by the SAME Ring 3 pass-4 human anchor-fidelity spot-check, not a future validator. Direction of error: a real omission can go un-warned; the check never produces a spurious warning for a genuinely-cited feature. **RETIRED BY CONSTRUCTION (S69 F-H close-out)**: `_check_feature_coverage` itself is retired, superseded by `claim_voicing.py`'s V-4 claim-coverage check, which operates per-CLAIM (not per-chunk-id-set) — closing this false-positive class natively, not patching around it. `_check_feature_coverage` is now dead code (see `palm_reading.py`'s own module docstring), not deleted.

- **`_DOGFOOD_CAPTURE` duplicated across two files** (S84) -- read
  independently in `frontend/app.py` and `agent/interpretive/palm_reading.py`
  (same env var `ASTRO_DOGFOOD_CAPTURE`, same `== "1"` comparison, not
  threaded as a parameter) -- duplicated but low-risk since both compute
  the constant once at import time in the same process.

Ayana Bala Kranti (RESOLVED Session 47), Sun Ayana Bala doubling (RESOLVED Session 47), and Bhava Dig Bala (resolved Session 42, 48/48 exact match) — full validation detail archived to SESSION_LOG.md's compression section. Older/narrower divergences (Saptavargaja scoring, Drekkana Bala, Ayana Bala Moon/Venus edge case, PDF-tooling gap, Sequencing lock violation) also archived there.

## Known Divergences

Production maintains documented divergences from commercial Vedic oracles (Drik Panchang, AstroSage, JHora) and classical primary sources where either (a) sources genuinely fragment, or (b) mechanism is unresolved but impact on V1 scope is bounded. Full register in docs/KNOWN_DIVERGENCES.md. Reopen individual gaps when: expert consultation clarifies mechanism, or V2 scope expansion makes gap user-visible.

## Carry-Forward / Open Items
Resolved entries are archived in SESSION_LOG.md (see each session's "Carry-forward resolved" blocks) — this list holds OPEN items only.
- **Misc small ride-along fixes** (not standalone prompts) — (a) `av_transit_scorer.py`'s generic "unknown transit_planet" ValueError needs its own design-reason text for Rahu/Ketu specifically, currently folded into the generic path (Session 54); (b) Session 56 logged a 2948-passed close but the verified pre-Session-57 baseline (commit `3993d79`) was 2972 — 24-test gap, root cause not investigated, not blocking, reconcile before trusting any future absolute baseline delta (Session 57); (c) `strength.py`'s D2 docstring worked example ("2022-23 Saturn+Rahu-in-Aquarius") doesn't hold empirically — a 1900-2030 scan found no real overlap; the D2 fail-closed MECHANISM is unaffected, only the illustrative citation is wrong, fix opportunistically next `strength.py` touch (Session 58).
- **arudha_lagna internals still open** (Session 59) — (a) `uncertainty_days` ownership: formatter hardcodes 0.0, `build_domain_profile()` dispatch prompt must ratify single source of truth (passthrough vs. literal) before wiring; (b) `arudha_lagna` payload passes through meta keys `tier`/`sources` — reconcile or strip when the next Jaimini domain lands (dual tier representation risk).
- **arudha_lagna co-lord cascade, lagna-level coverage still zero** (flagged S57/S59) — none of the 4 canonical reference charts has a Scorpio/Aquarius Lagna, so `strength.py`'s `stronger_co_lord()` cascade (the co-lord-resolution path `arudha.py` calls whenever `house_sign` is co-lorded) has never been exercised by a real chart at the ARUDHA-LAGNA level end to end — coverage is `strength.py`'s own unit tests plus a monkeypatched-`swe.calc_ut` synthetic fixture only; no new reference chart planned to close this. **PARTIALLY CLOSED (S63)**: house-12-level cascade IS now real-chart-verified (Sulabh UL: Scorpio house sign, Ketu via `step_2`, `test_orchestrator_upapada.py`) — only the lagna-level half stays open.
- **Stale prose needing refresh** (opportunistic, not standalone prompts) — (a) `golden_harness.py`'s `_KNOWN_GAPS`: 5 entries' "Session 50 observed mechanism" text describes pre-Session-55 routing behavior, now superseded by the `route`-field split (MATCH vs MATCH_STAGE2) (Session 55); (b) `golden_qa_sulabh.py`'s `sulabh_arudha_q2_stage2` `baseline_answer_summary` explains a now-obsolete verbatim-collision-avoidance rationale — obsolete since the harness reads `DomainAnswer.route` directly on the success path, only the ERROR path still needs the old correlation (Session 64).
- **Router tuning backlog** (dogfood-gated per the Session 44 lock — tune only with Answer Scorecard evidence, not preemptively) — (a) "will my marriage be happy"/"are we compatible" router-only probes (no partner chart) classify `marriage_compatibility` at HIGH confidence but correctly REFUSE via the `has_partner_data` hard guard — not a routing-quality miss, noted so a future reviewer doesn't misread these 2 "lost" rows in the S61 layman-reachability count (Session 61); (b) bare `"when"` in `_DASHA_KEYWORDS` is a generic-token smell, same class as the pre-existing "job"/`_STEM_MAP` dead-keyword bug — a candidate question scored a false Stage-1 `current_dasha` hit purely off the bare token (Session 64).
- **Post-V1 design gate: Lal Kitab remedy tier** (Session 61) — a possible future tier surfacing Lal Kitab remedies, scoped RAG-grounded and cited, never prescriptive (no "wear this gemstone" style output) — requires, before ANY wiring: a rewrite of golden-set row R5 (`sulabh_refusal_boundary_probes_r1_r5`'s gemstone-remedy sub-probe currently asserts REFUSAL under CLAUDE.md's "remedies are out of V1 scope entirely" posture), a V1-scope amendment to this file's own Locked Decisions, and design-chat consensus. Not started; recorded here as a placeholder so it isn't proposed as a quick add later without the gating steps.
- **prompt_builder.py kundali-slot instruction drift** (Session 65) — the kundali-slot instruction ("go through each antardasha sub-period one by one with dates") violates the ±37-day drift posture + pratyantar suppression lock — MUST be audited before any future use of the kundali slot; no edit now (slot unused by the V1 T4 path; ask() quarantined).
- ask()/prompt_builder/context_classifier quarantine residue in app.py — inventory + retirement decision, V1.1.
- `astrosage_parser.py` next touch = noise-strip (linearized two-column tables, page footers, promo lines, doubled-character bold artifacts) + a public `SECTION_NAMES` alias.
- **V1.1 register** — contrast preprocessing before vision-model description (evidence: Ring 3 pass-2 P7 finding — LEFT's `FATE LINE: Barely visible` field vs. the user's physical "lightly visible" read, a photo-lighting delta, Session 66); checkpoint plain-language field glosses (a layperson reviewing "FATE LINE: Barely visible" may not know what a fate line signifies before confirming it, Session 66); layman progressive disclosure (surface interpretive depth gradually rather than one dense reading paragraph, Session 66); AppTest-in-CI proposal (Streamlit's `AppTest` harness could catch nested-expander-class crashes before live dogfood, Session 66); golden fixture rows with `baseline_source: "astrosage_kp_2026-07"` (career-timing domain) carry two uncorrected capture errors — Ketu-Sun antardasha mislabeled as Venus Mahadasha, 10th lord stated as Moon vs. computed Mercury (Session 68); splitter coverage broadening — the live 7-keyword `astrosage_parser.py` splitter captures 6/7 of its own target sections and misses `Transit Today` entirely on the audited PDF, while the PDF's actual full taxonomy has 31 sections (audit: `diagnostics/astrosage_coverage_audit_S68.md`) — broadening the splitter's keyword coverage is a V1.1 candidate, not decided here (Session 68); dogfood `## RUN` block copy-by-timestamp script — the current manual copy-paste-from-`read_prompt.md` workflow for Ring 3 scoring passes is paste-friction, a small script keyed on RUN timestamp would remove it, low priority (Session 68); corpus re-ingestion/chunk-repair (Session 68 F-C) — `diagnostics/fc_heartline_corpus_S68.md` found a genuine chunking gap (p.157-158, zero chunks) and a whitespace-normalization need (`p159_c2`'s "first\nfinger" line-wrap defeats literal substring matching); RIDER: any re-ingestion work must also revisit `CHUNK_ANCHOR_TAG_PATTERN`'s coupling to the current `*_p<n>_c<n>` id schema in the SAME change, since a different id shape would silently break A1 tagging (Session 68); promote `valid_chunk_ids` onto `PalmReadingResult` (Session 68 F-C) — would let the F5 capture record the real count/membership instead of `"unavailable"`, closing accepted gap (e) above; HAND_DETAIL fate-line word-order miss (Session 68 F-B) — `"There is no clearly visible fate line in the image."` (real HAND_DETAIL text, Run 3) stays unmatched by BOTH `_ABSENCE_PHRASES` and `_ABSENCE_PATTERNS_BY_FEATURE` because its noun ("fate") sits AFTER "visible", the reverse of F-B's `no <noun> ... visible` target shape — a real but narrower, DIFFERENT word-order gap than the one F-B was scoped to fix. Currently harmless (fate line resolves correctly from its other 2 sources regardless); fix = an additional reversed-order pattern variant, only if a future dogfood pass shows this specific ordering recurring and causing an actual misclassification; `test_palm_reading.py` monolith split (1694 lines after P5b) — candidate split points already implicit in this file's own section comments (retrieval/support-gate direct-unit-tests vs. two-stage integration vs. retired-validator inventory), not decided here (Session 69); prompt-drafting rule for future large test-alignment prompts (Session 69) — inline the fixture-builder spec (helper signature + 2-3 worked examples) directly in the instructing prompt when touching >15 tests in one pass, and prefer targeted test runs (isolate the file under edit, iterate) with a single full-suite run only at the end, not per-edit — P5b's own practice, worth normalizing as an explicit rule rather than an implicit habit; parallelize Stage-1 per-feature extraction calls (Session 70) — `claim_extraction.extract_claims` issues its per-feature calls SEQUENTIALLY, adding roughly 10s of live dogfood latency; each feature's call is independent by construction (no cross-feature state), so this is a mechanical concurrency win, not a design question, deferred only for lack of a dedicated prompt; V-5 `[FLOW]` false-positive on legitimate summary-closing sentences (Session 70 F-G4 register) — remains an ACCEPTED gap after the `[OBS]` carve-out (a pure connective/closing sentence that happens to name a feature-noun still fails even when it isn't truly interpretive), a future refinement candidate, not attempted at F-G4; needle-inventory audit (Session 70 F-E finding) — `_FEATURE_TRAIT_NEEDLES`/`_SUPPORT_NEEDLES`'s singular-noun needles (e.g. `"finger"`, `"mark"`) cannot match a plural-only surface form under `\b(...)\b` word-boundary matching on their own; currently masked because `_SUPPORT_NEEDLES` already carries both singular and plural forms for every feature that needs them, so no observed failure exists yet, but the underlying single-word matching design doesn't generalize to a needle set that ever drops the plural form; palm_reading.py S65 flags, trigger-based, MOVED here from a standalone Carry-Forward bullet at S71 (Option Z palm drop) -- (a) imports `DISCLAIMER` from legacy `prompt_builder.py`, should relocate to a neutral constants home, trigger = any `prompt_builder.py` retirement/relocation work; (b) module-level `OpenAI` import defeats conftest autouse stubbing (explicit `client=` injection covers current tests), move import inside `generate_palm_reading()`, trigger = next `palm_reading.py` touch; F-D retrieval instability, MOVED here from a standalone Carry-Forward bullet at S71 -- still queued/unblocked/undebated (design debate, no code yet), untouched by F-H (F-H changed how retrieved chunks get COMPOSED into a reading, not how they get RETRIEVED); pass 3 found the SAME claim C-supported in Run A/B but U in Run C (HAND_DETAIL shifts the query enough to drop the chunk), pass 4 reconfirmed the SAME instability class on a different claim; `diagnostics/fc_retrieval_probe_S68.md`'s LR-vs-LRH scopes remain the evidence base; design debate, not a quick fix; `palm_processor.py` rider, MOVED here from a standalone Carry-Forward bullet at S71, still open, unchanged scope -- R1 shipped a bullet-format parser for `describe_hand_detail_image`'s CURRENT output shape; the prior "align to F4 flat-field format" instruction, if executed as a standalone next-touch, would silently break R1's hand_detail feature extraction; must be re-scoped as a COORDINATED two-file change (processor prompt + palm_reading parser) in a future design-chat session; revisit palm reading with S65-S71 findings (S71, new) -- Stage-1 valence classification is task-model-fit-limited on classical hedged prose (see the V1 PALM DROPPED Locked Decision entry); consider a structured-display fallback (verbatim Cheiro citations, no LLM valence judgment) or a hand-curated whitelist mode as V1.1 candidate remedies, neither designed or decided here; **offline-verified-extraction pilot (this session, restructuring the palm-retrieval follow-up)** -- Cheiro palm reading moves from live per-request Stage-1 extraction (fragile: `empty_first`/raw=0 outcomes and 0.4-overlap-floor rejections judged fresh on every call with no verified ground truth, diagnostics/KNOWN_PATTERNS.md P-016) to OFFLINE, human-verified extraction into a rules table, scoped BOOK-WIDE rather than chapter-grouped so compound/cross-chapter conditions (head+heart line combos, diagnostics/KNOWN_PATTERNS.md P-015) can be captured at all. This SUPERSEDES the previously-planned Stage-1 overlap-floor threshold investigation (SESSION_LOG.md S84's "next lead") -- do not additionally pursue that thread, it is replaced by this pilot, not run in parallel with it. Framed explicitly as a PILOT: validates the offline-extraction method on Cheiro/palm before any astrology-book (BPHS/Phaladeepika/Saravali-class) application is considered -- see the V1.5 register entry below for the cross-link to the generalized rule-engine gate, which this pilot does NOT unlock on its own.
- **V1.5 Rule Engine Foundation** — PROPOSED, gated on 4 prerequisites, not ratified. See Locked Decisions entry for prerequisites + design pointer (`playbook_export/decisions/rule-engine-v1_5-design.md`). Do NOT open V1.5.1 until all four are confirmed green.

- **S82 open items** — (a) `p123_c0` nomenclature attractor, DATA, latent behind the gate for ranged features and live for any unranged one; census structural chunks before choosing a `page_type` exclusion vs a chunking change; (b) gate/OCR-corruption interaction — 32 of 51 line-family corruptions sit inside gated ranges and the gate raises exposure by shrinking the competing pool, direction certain and magnitude UNMEASURED; (c) `_build_where` accepts `bool` as `int`, fix with `type(v) is int`, unreachable by current callers; (d) nothing pins the shipped value of `_FEATURE_PAGE_FILTER_ENABLED` — add a one-line test and rename `test_page_range_gate_off_by_default_omits_page_ref_key`; (e) `sun line` gate behaviour UNMEASURED, inferred safe from the a69549a census; (f) `requirements.txt` is a one-line stub in a public repo that is now the distribution artifact; (g) PRIVACY, user-deferred — `data/default_user/kundali_summary.txt` and 5 `data/sessions/*.json` tracked in a public repo against the no-storage lock, needs a second history rewrite; (h) `data/all_chunks.json` and `data/chunked_chunks.json` history bloat, ~22 MiB of generated-artifact revisions.

## Carry-forward corrections
- S72 close carry-forward incorrectly stated "Prompt 3 (router wiring) committed and pushed" — actually uncommitted in working tree, caught S73 open by `git log origin/main..HEAD --oneline` returning empty against expectation. Landed as S73 Commit A (router + Stage 2 test substitution). Full detail: SESSION_LOG.md S73.


## Rule Authoring & Audit Architecture
Storage convention is authoritative in `data/palm_rules/README.md` -- read it there, not restated here.

- **Standing rule**: every chapter follows the same pipeline -- doctrine inventory (`_doctrine/doctrine_<line>.md`) -> rules (`palm_rules_<chapter>_v1.json`) -> reconciliation audit (`_audit/reconciliation_<line>.md`) -> unauthorable register (`unauthorable_register.json`). Authoring is complete for a chapter only when every doctrine statement maps to either a verified rule OR a register entry -- no doctrine statement may go unaccounted for.
- **MANDATORY reading** before any rule-authoring or reconciliation work (palm now, astrology later): `data/_meta/learnings_for_astrology_rules.md` -- the reusable procedure and the three-vocabularies-drift mistake class it exists to stop from repeating chapter to chapter.
- **Status**: head line reconciliation is DONE (`data/palm_rules/_audit/reconciliation_head.md`). Heart line is the next audit. All other chapters are backlogged for a future batch sweep, not started.

### VERIFICATION ARCHITECTURE — fidelity-not-truth (adopted 2026-08-08; prior-art confirmed)

**Standing spec:** `Prior-Art_Investigation_Deterministic_Citation.md` + `VERIFICATION_ARCHITECTURE.md` (project files); `VERIFICATION_ARCHITECTURE.md §12` = next-steps plan. Read both before any rule-authoring or unauthorable decision.

**Guarantee = FIDELITY, not TRUTH.** No palmistry/astrology expert is in the loop, so the system guarantees only that a claim is faithful to the cited source text ("Cheiro p.X states Y"), never that it is doctrinally correct. Truth is abandoned as a guarantee. Product voice surfaces attribution, never "you will experience Y".

**Scaling move:** verify the predicate VOCABULARY (T-box — bounded, closed, one-time) by hand; individual rules (A-box) scale by constrained mapping onto that verified vocabulary. Anything that will not map is QUARANTINED, never guessed. Rules are EXTRACTIVE: condition/consequence are verbatim character-spans (offsets) of the source, not paraphrase. Isomorphism (one rule <-> one source fragment) + defeasible/prioritized-default precedence (specific modifier > base rule > honest decline).

**§6 MECHANICAL CHECKS (CI, zero human time — the recurrence guard):** (1) every source_span resolves to real text; (2) condition/consequence lexically grounded in the cited span; (3) NO rule may cite a span tagged `scope`; (4) every askable feature has >=1 operative rule or the chapter fails sign-off; (5) contradiction: same feature, opposite valence, both `base_rule` -> flag; (6) vocabulary closure. Sentence roles = scope / base_rule / modifier / ambiguous. Competency questions per feature = coverage gate.

**ROOT-CAUSE LOCK:** rule-authoring and unauthorable decisions are made against the WHOLE CHAPTER read in order (or verbatim spans), NEVER a retrieved chunk or single page. A framing/scope sentence read in isolation is the recurring source of both fabricated rules and false "no rule exists".

**HEAD-LINE CORRECTION (supersedes the S71 "strength or weakness" valence-landmine rationale as applied to the head line):** Cheiro p145 "the line of head relates principally to the mentality of the subject — to the intellectual strength or weakness..." is a CHAPTER SCOPE/TOPIC sentence, not a disjunctive rule and not grounds for unauthorable. p146-148 carry clean hand-type-INDEPENDENT base rules (short -> material/practical; broken on both hands -> head injury; island -> weakness; offshoot to a Jupiter star -> success; connected to life line at start -> caution/sensitivity, the real basis for H_001/H_002 and "close to life line"). p149 is a SEPARATE chapter (VIII): hand-type is a MODULATION layer, not a precondition. The head line is UNDER-COVERED, not unauthorable; first authoring target once the reference-arm method is proven.

**AI-REVIEW nuance (updates Working Style #5):** AI-checking-AI is PERMITTED to COMPRESS human review (ensemble extraction, round-trip regenerate-and-compare, reference-arm probes run per-layer with pinned upstream inputs) but (a) the frozen sign-off is a non-delegable human act and (b) an AI reference-arm's output never becomes authored content — it yields discrepancy TICKETS with source anchors that the human confirms mechanically.

**CORPUS SCOPE GUARD:** do NOT build the heavy vocabulary/quarantine/active-learning machinery for Cheiro (~140 rules, 5-8 hrs once — mechanical checks + ensemble triage + review). Full machinery is for the astrology track (BPHS/Phaladeepika/Saravali, ~10x larger). RETRACTED (no expert to drive them): ripple-down-rules, GRADE recommendation-strength, active-learning-on-doctrinal-uncertainty; retarget active learning at extraction uncertainty.
