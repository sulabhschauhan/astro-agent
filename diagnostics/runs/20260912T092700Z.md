# S127 — David characterization regression test: STOPPED at step 1

**Decision outcome: STOP.** No regression test was created. This is not
a drift finding (production did not move) — it is a missing-artifact
finding: the "golden dict already reviewed" that step 1 asks to diff
against does not survive anywhere in the repo to diff against.

## Step 0 — branch

`git branch --show-current` → `wip/interpretive-pilot` (confirmed).
`git status` — clean pre-existing state (the usual large pile of
untracked session artifacts from prior sessions, plus `CLAUDE.md`,
`SESSION_LOG.md`, `claude_handover_S126.md`, `docs/KNOWN_DIVERGENCES.md`
already modified by earlier tasks this session — none touched by this
task).

## Step 1 — re-capture and compare: BLOCKED before the diff could run

**Locating the capture input (unambiguous — not the blocker):**
`grep -ril "s127" diagnostics/ | grep -i david` found exactly one script,
`diagnostics/s127_chart_calculator_david_capture.py`. Its own docstring
and `DAVID_INPUT` dict confirm the input verbatim:
`{"name": "David", "dob": "19 Jan 1976", "tob": "22:00", "place": "London, UK"}`,
sourced from `tests/test_chart_calculator.py` `_MUNTHA_FIXTURES` — the
same canonical David input used across ~14+ other call sites per
`diagnostics/vimshottari_year_length_S74.md` §7.3. This part of the task
is unambiguous; no guessing was needed or done.

**The actual blocker — the golden dict has no surviving copy:**
the script writes its captured output ONLY to `diagnostics/latest_run.md`
(`OUT_PATH` in the script, line 58). That file is OVERWRITE-ONLY by
project convention (CLAUDE.md Diagnostics & Reporting Conventions) and
is `.gitignore`'d (confirmed: `.gitignore:41` lists it), so it has never
been committed and carries no history. Since the original S127 capture
was produced ("a prior task this session," per
`reference/oracle_fixtures/david.md` §10d), `diagnostics/latest_run.md`
has been overwritten **twice more** by later tasks in this same session
(the David/Sheridan oracle-reconciliation manifest, then the CLAUDE.md
Working-Style rules 32-34 run log) — both writes are legitimate,
correctly-scoped uses of the same overwrite-only file, not mistakes to
undo. `david.md` §10d already flags this explicitly in its own text:
"that capture has since been overwritten by this manifest... re-run the
S127 capture script if the raw dict is needed again." No other file in
the repo holds a full verbatim copy of the original reviewed dict —
confirmed by grepping for `23.5226` / `ayanamsha_lahiri` (the one value
known to have appeared in it) across `*.md`/`*.py`/`*.json`: only
`agent/chart_calculator.py` (source, not a capture), the two
oracle-reconciliation files (`reference/oracle_fixtures/{david,
sheridan}.md`), the two manifests, `vimshottari_year_length_S74.md`
(a different, S74-era diagnostic), and `tests/manual/timezone_wiring_
check.py` (unrelated) — none of these embeds the full per-body
`{sign, house, dignity, retrograde}` set this test needs to hard-code.

**Action taken given the blocker:** re-ran the capture script
(`python diagnostics/s127_chart_calculator_david_capture.py`) to obtain
a **fresh, current** `calculate_chart()` output for David, strictly for
the record — this is explicitly NOT treated as a newly-reviewed golden
baseline, since no human has looked at it in this task. The script is
read-only with respect to production (its own docstring: "READ-ONLY
probe -- no production file is modified," confirmed by `git status`
showing zero changes under `tests/`, `agent/`, `data/pdfs/` after the
run). It succeeded without exception and wrote to `diagnostics/
latest_run.md` (captured at `2026-09-12T09:26:14Z`). Since this report
also needs to occupy `latest_run.md` per rule 26, the raw capture was
additionally archived verbatim to `diagnostics/runs/
20260912T092614Z_david_capture_raw.md` before this report's own `cp`
overwrote `latest_run.md` — the raw dict is not lost.

**One useful corroboration, not a substitute for review:** the fresh
capture's `meta.ayanamsha_lahiri = 23.5226` matches exactly the value
already cited as David's ratified ayanamsa in
`reference/oracle_fixtures/david.md` (from the earlier oracle
reconciliation task, itself sourced from "the S127 `calculate_chart()`
meta.ayanamsha_lahiri capture"). That one field is consistent across
this session's separate captures — but a single matching field is not
equivalent to a full, human-reviewed golden dict for the other 8
bodies' `sign`/`house`/`dignity`/`retrograde` values plus `lagna_chart`,
which is what step 2's test needs.

## Step 2 — NOT executed

Per the task's own decision framing ("if fresh output matches golden
values exactly, lock as a regression test; else STOP and report the
diff"), the precondition — an existing, human-reviewed golden dict to
diff against — is unmet. Fabricating one now from this session's own
unreviewed fresh capture, or from any prior-turn system-reminder
excerpt, would violate the newly-added CLAUDE.md Working Style rule 32
("SUMMARIES AND MEMORY ARE LEADS, NOT AUTHORITY" — never assert a
repo/state fact from a summary or memory; trace to the primary source)
and this task's own "do NOT invent input" / STOP-if-can't-locate-
unambiguously instruction, extended here to the golden comparison
artifact, not just the input tuple.

No test file was created. `tests/regression/` was not touched.

## Files touched this run

- `diagnostics/latest_run.md` — overwritten twice this run: first by
  the capture script (fresh David `calculate_chart()` dict), then by
  this report (per rule 26's "write to runs/, then cp to latest_run.md"
  convention). Its current, final content is this report, not the raw
  dict.
- `diagnostics/runs/20260912T092614Z_david_capture_raw.md` — the raw
  fresh capture, archived here so it survives this report's own
  overwrite of `latest_run.md`.
- `diagnostics/runs/20260912T092700Z.md` — this file.
- No production, fixture, or test file was created or modified.

## Recommendation

Two options for Sulabh, neither taken here:
1. Review the fresh capture archived at `diagnostics/runs/
   20260912T092614Z_david_capture_raw.md` (captured 2026-09-12T09:26:14Z)
   as the new golden baseline, then re-run this exact task — step 1
   will then have something real to diff against (itself, trivially
   clean) and step 2 can proceed.
2. If a prior review of the original S127 capture happened outside this
   repo (e.g. read directly from a chat transcript rather than saved),
   supply those exact values back so they can be committed somewhere
   durable (not `latest_run.md`) before this task is re-attempted.

---

STOPPED at step 1 — missing golden-comparison artifact, not a
production drift. AWAITING HUMAN REVIEW / re-supply of golden values.
