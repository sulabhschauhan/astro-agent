# S127 — commit the 1b longitude edit + its characterization test

**Commit: `a65cb4c`.** "feat(chart): surface per-planet sidereal
longitude in calculate_chart() public output + lock David
characterization (S127 1b); additive, drift-guarded, Camp-Y geometric
(JHora matched-mode residual <=30.7\" David, documented Gap A1)"

## Step 0 — branch

`git branch --show-current` → `wip/interpretive-pilot` (confirmed).

## Step 1 — stage exactly two files

`git add agent/chart_calculator.py tests/regression/test_chart_
calculator_characterization_david.py` (explicit paths, not `-A`/`.`).

`git diff --cached --name-only` →
```
agent/chart_calculator.py
tests/regression/test_chart_calculator_characterization_david.py
```
Exactly those two paths, nothing else. No STOP condition triggered.

## Step 2 — pre-commit sanity

`git status --short` filtered to staged (non-`??`) entries:
```
 M CLAUDE.md
 M SESSION_LOG.md
M  agent/chart_calculator.py
 M claude_handover_S126.md
 M docs/KNOWN_DIVERGENCES.md
M  tests/regression/test_chart_calculator_characterization_david.py
```
The two `M ` rows (staged) are exactly the intended files. All other
rows are ` M` (unstaged, first column blank) — pre-existing working-
tree modifications from earlier tasks this session, untouched by this
task's `git add`. `reference/` (the oracle docs) and the large
untracked pile remained unstaged throughout. Sanity check passed.

**Quick guard:** `pytest tests/regression/test_chart_calculator_
characterization_david.py -q` → **25 passed**, 0 failed. Proceeded to
commit.

## Step 3 — commit

`git commit -m "feat(chart): surface per-planet sidereal longitude in
calculate_chart() public output + lock David characterization (S127
1b); additive, drift-guarded, Camp-Y geometric (JHora matched-mode
residual <=30.7\" David, documented Gap A1)"` (plus the standing
attribution trailer) → **commit `a65cb4c`**, "2 files changed, 59
insertions(+), 3 deletions(-)".

## Step 4 — verification

- `git log -1 --oneline` → `a65cb4c feat(chart): surface per-planet
  sidereal longitude in calculate_chart() public output + lock David
  characterization (S127 1b); additive, drift-guarded, Camp-Y
  geometric (JHora matched-mode residual <=30.7" David, documented Gap
  A1)` — hash and message confirmed.
- `git status --short` → neither `agent/chart_calculator.py` nor the
  test file appears anymore (fully committed, clean of both); the
  pre-existing `M`-modified docs (`CLAUDE.md`, `SESSION_LOG.md`,
  `claude_handover_S126.md`, `docs/KNOWN_DIVERGENCES.md`) and the large
  untracked pile from prior sessions/tasks are unchanged and untouched.
- **Not pushed** — no push instruction was given, none performed.

---

Committed: `a65cb4c`. Two files, nothing else. 25/25 pass. Not pushed.
