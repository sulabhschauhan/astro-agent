# CLAUDE.md Working Style — rules 32-34 appended (S127)

Branch: `wip/interpretive-pilot` (confirmed before any write).

Idempotency guard: `grep -ic "SUMMARIES AND MEMORY ARE LEADS" CLAUDE.md`
→ 0 matches. Not already present — proceeded.

Patch method: Python exact-string replacement (no full-file read via the
Read tool, per rule 15). Anchor `## Varshaphal House-Counting Convention
(Session 18)` pre-replace count asserted == 1 before writing; post-write
count re-asserted == 1 (anchor line preserved, new block inserted
directly above it). All-or-nothing: write only occurs after both
assertions pass.

**Result: assert_count = 1 (both pre and post). byte_delta = +1377.**

## Rules added

32. **SUMMARIES AND MEMORY ARE LEADS, NOT AUTHORITY**
33. **DESIGN-INTENT-FIRST + KNOWN-PATTERNS BIND THE DESIGN CHAT**
34. **DESIGN CHAT READS WHAT IT CAN READ**

Docs-only edit, commit-exempt per rule 14 (no ratification token
required). No source/fixture/test file touched. No read-back of
CLAUDE.md performed (assertion counts + byte delta are the verification,
per rule 15).
