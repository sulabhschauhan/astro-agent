# S127 — David characterization regression test: LOCKED

**Decision outcome: fresh capture matched the supplied golden values
exactly → test written and passing.** No production drift detected.

## Step 0 — branch

`git branch --show-current` → `wip/interpretive-pilot` (confirmed).
`git status` — pre-existing modified/untracked state from earlier
sessions/tasks (`CLAUDE.md`, `SESSION_LOG.md`, `claude_handover_
S126.md`, `docs/KNOWN_DIVERGENCES.md` modified; large pile of untracked
diagnostics/scripts/probes from prior sessions) — none touched by this
task.

## Step 1 — fresh capture vs supplied golden: CLEAN

Re-ran `diagnostics/s127_chart_calculator_david_capture.py` (read-only
probe, no production file modified — confirmed by `git status`
afterward). Parsed the fresh `calculate_chart()` dict and compared
against the golden values supplied in the instructing prompt.

**Full compared field table (16 items, all MATCH):**

| Field | Golden | Fresh | Status |
|---|---|---|---|
| lagna_chart.ascendant | Virgo | Virgo | MATCH |
| lagna_chart.ascendant_lord | Mercury | Mercury | MATCH |
| lagna_chart.rasi | Leo | Leo | MATCH |
| lagna_chart.rasi_lord | Sun | Sun | MATCH |
| lagna_chart.nakshatra | Magha | Magha | MATCH |
| lagna_chart.nakshatra_pada | 4 | 4 | MATCH |
| lagna_chart.nakshatra_lord | Ketu | Ketu | MATCH |
| Sun (house,sign,dignity,retro) | 5, Capricorn, Inimical, False | 5, Capricorn, Inimical, False | MATCH |
| Moon | 12, Leo, Friendly, False | 12, Leo, Friendly, False | MATCH |
| Mars | 9, Taurus, Neutral, True | 9, Taurus, Neutral, True | MATCH |
| Mercury | 5, Capricorn, Neutral, True | 5, Capricorn, Neutral, True | MATCH |
| Jupiter | 7, Pisces, Own Sign, False | 7, Pisces, Own Sign, False | MATCH |
| Venus | 3, Scorpio, Neutral, False | 3, Scorpio, Neutral, False | MATCH |
| Saturn | 11, Cancer, Inimical, True | 11, Cancer, Inimical, True | MATCH |
| Rahu | 2, Libra, Neutral, True | 2, Libra, Neutral, True | MATCH |
| Ketu | 8, Aries, Neutral, True | 8, Aries, Neutral, True | MATCH |

**Result: CLEAN — zero diffs.** Production has not moved since the
original S127 capture (also consistent with the prior task's finding
that `meta.ayanamsha_lahiri` independently matched across captures).

## Step 2 — regression test written

New file: `tests/regression/test_chart_calculator_characterization_
david.py`. Hard-codes the 16 supplied golden values as
`GOLDEN_LAGNA_CHART` / `GOLDEN_PLANETARY_POSITIONS` dicts, each preceded
by the required comment: "GOLDEN: S127 human-reviewed characterization
capture, cross-verified against a fresh capture on 2026-09-12. Locks
CURRENT behavior of the zero-coverage production D1 path; NOT an
oracle. sign/house/dignity/retrograde only -- longitudes deferred to
1b." Two parametrized test classes assert per-field
(`TestLagnaChartCharacterization`, one case per lagna_chart field) and
per-planet (`TestPlanetaryPositionsCharacterization`, one case per
planet asserting all 4 fields), each failure message naming the exact
body + field. No longitude, dasha/pratyantar, meta, or aspects
assertions, per scope. Reuses the existing session-scoped, autouse
`_patch_geocoder` fixture in `tests/conftest.py` — no manual Nominatim
stubbing needed in the new file. No `__init__.py` added (pytest
collected the new file correctly without one; other `tests/*` package
dirs have one, but adding one here would be a second new file — not
added per the "one new test file" surgical constraint, and collection
worked without it).

## Step 3 — test runs

- New file alone: `pytest tests/regression/test_chart_calculator_
  characterization_david.py -v` → **16 passed** (7 lagna_chart fields +
  9 planets), 0 failed.
- Full suite: `pytest -q` → **3954 passed, 7 skipped**, 0 failed, in
  105.71s. Prior committed baseline (CLAUDE.md S126 close-out): 3938
  pass / 7 skip / 0 fail. **3938 + 16 = 3954 — exact match, confirming
  the only suite change is this new file's 16 parametrized cases.**

## Scope confirmation

`git status --short -- tests/ agent/ data/pdfs/ data/palm_rules/` after
all work showed only:
```
?? tests/regression/
```
No production, fixture, or stub file was modified. **Not committed** —
awaits "RATIFIED: commit authorized" from Sulabh after review, per rule 14.

---

Test locked and green. AWAITING HUMAN REVIEW / commit ratification.
