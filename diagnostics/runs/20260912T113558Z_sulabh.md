# S127 Run Log — Sulabh Oracle Reference, 2nd Pass (matched-mode §3e supplied)

Branch: `wip/interpretive-pilot` (unchanged since 1st pass). NO commit
made. This is an UPDATE pass, not a rebuild.

## STOP-IF-EXISTS check

`reference/oracle_fixtures/sulabh.md` already existed (built earlier
this session, 1st pass). Per the new standing rule, did NOT
rebuild/clobber — switched to update-mode: targeted `Edit` calls only,
no `Write` overwrite of the file. Reported this at the end of the
1st-pass turn's context; proceeding here per the user's explicit
follow-up instruction to run Sulabh's update now and show it before
Surbhi.

## MOD 1 — §3e built from user-supplied matched-mode data

User supplied JHora v8 Traditional Lahiri DMS positions for 9 grahas +
Lagna (Rahu/Ketu Mean-node convention stated by the user, confirmed
below). Converted DMS → abs 0-360° by hand-verified Python arithmetic
(sign-start-offset + d + m/60 + s/3600).

Ran `calculate_chart("Sulabh", "6 Apr 1988", "00:30", "Calcutta, India")`
— canonical tuple confirmed unambiguous via
`tests/calculations/strength/test_drik_bala.py:220` (and 2 other
byte-identical call sites), not invented. Read
`planetary_positions[<planet>]["longitude"]` for grahas and
`meta.asc_lon_sidereal` for Lagna.

### Per-planet Δ arcsec (production − JHora Trad-Lahiri)

| Body | Δ arcsec | Flag? |
|---|---|---|
| Lagna | 38.62 | no |
| Sun | -20.63 | no (aberration-scale, matches David's -20.94″) |
| Moon | -1.10 | no |
| Mars | -18.87 | no |
| Mercury | -47.92 | no (max observed) |
| Jupiter | -28.64 | no |
| Venus | -13.98 | no |
| Saturn | -1.79 | no |
| Rahu | -0.14 | no — node-convention match |
| Ketu | -0.14 | no — node-convention match |

All sub-90″, none reached the 100″+ FLAG threshold. Rahu/Ketu ≈-0.14″
confirms Mean-Node convention agreement between production and this
JHora capture. Recorded as Camp-Y (Gap A1 family), not drift — same
disposition as David's/Sheridan's §3e tables.

Full DMS→abs conversion table and the resolution write-up are in
`reference/oracle_fixtures/sulabh.md` §3e (verbatim; not duplicated
here). The prior §3-CONFLICT resolution row and the §0 mode-discipline
note were both updated in place to point at §3e instead of describing
it as an open external ask.

## MOD 2 — Surbhi-specific flags: N/A for this file

MOD 2's three flags (nakshatra mislabel, combustion Basics-view
invalidation, Saturn retrograde FLG_SPEED fix) are Surbhi-specific per
the user's own framing ("SURBHI flags now RESOLVED") — nothing to apply
to Sulabh's file. Noted here only to record that this was checked, not
skipped by oversight.

## MOD 3 — output routing

Per explicit instruction ("nothing else is running now ... DO write
latest_run"): this file written to `diagnostics/runs/` first, then
copied verbatim to `diagnostics/latest_run.md` via a plain file copy
(not re-rendered) immediately after this Write, per Working Style #26
("Copying is a shell command, not a second render").

## Files touched this pass

- `reference/oracle_fixtures/sulabh.md` — 3 `Edit` calls (§0 note, new
  §3e section + CONFLICT resolution row, "Step 4 NOT RUN" → RESOLVED
  paragraph). No full-file rewrite.
- `diagnostics/manifest_sulabh.md` — 3 `Edit` calls (item 3 resolution
  upgraded to full closure, external ask #1 marked CLOSED, header
  conflict-count line updated).
- `diagnostics/runs/20260912T113558Z_sulabh.md` — this file, new.
- `diagnostics/latest_run.md` — overwritten with a copy of this file
  (per MOD 3; see note above — first pass this session did NOT touch
  this file, per that pass's explicit no-touch instruction; this pass's
  instruction supersedes it).

No git commit made. AWAITING HUMAN REVIEW before Surbhi's 2nd pass
begins, per the user's explicit sequencing instruction.
