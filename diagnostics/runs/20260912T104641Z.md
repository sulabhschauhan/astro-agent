# S127 1b — surface per-planet longitude (additive) + measure vs JHora, David

**No commit.** Awaits "RATIFIED: commit authorized" after Sulabh
reviews the measured deltas below.

## Step 0 — branch

`git branch --show-current` → `wip/interpretive-pilot` (confirmed).
`git status` — pre-existing modified/untracked pile from earlier
sessions/tasks, unrelated to this task; nothing else staged or touched.

## Step 1 — read before editing (rule 34)

`agent/chart_calculator.py::_calc_planets()` (line 398) already
computes each planet's absolute sidereal longitude and retains it
internally:

```python
flags = swe.FLG_SWIEPH | swe.FLG_SIDEREAL | swe.FLG_SPEED
...
xx, ret = swe.calc_ut(jd_ut, pid, flags)
lon = xx[0] % 360
...
planets[name] = {"longitude": lon, "sign": sign, "house": ..., "dignity": ..., "retrograde": ...}
```

This value is even consumed internally elsewhere in the same module
(`moon_lon = planets["Moon"]["longitude"]` at line 674 for nakshatra;
`rahu_lon = planets["Rahu"]["longitude"]` in `_calc_yogas()` for the
Kalsarpa check) — it is NOT discarded at the `_calc_planets()` layer.

**The discard happens one layer up**, in `calculate_chart()`'s own
`planetary_positions` dict comprehension (lines 710-718 pre-edit): it
copies only `house`/`sign`/`dignity`/`retrograde` from each planet's
internal dict into the PUBLIC return value, dropping `longitude` on
the floor.

**Convention confirmed:** absolute sidereal longitude, 0-360 degrees,
float, Lahiri ayanamsa (`swe.set_sid_mode(swe.SIDM_LAHIRI)` set at the
top of `calculate_chart()`, `FLG_SIDEREAL` flag on the `calc_ut` call —
not degree-in-sign).

**Conclusion: the "retained, not re-derived" path applies.** No new
ephemeris call was needed or made.

## Step 2 — surgical additive edit

Edited exactly one production function, `calculate_chart()`, in
`agent/chart_calculator.py`:

1. Added a small helper `_surface_longitude(planet, planet_data)`
   immediately before `calculate_chart()` — reads
   `planet_data["longitude"]`, normalizes with `% 360.0`, wrapped in
   `try/except (KeyError, TypeError, ValueError)` raising a
   `RuntimeError` naming the planet and the offending dict if the key
   is missing or malformed.
2. Added one line to the existing `planetary_positions` dict
   comprehension inside `calculate_chart()`:
   `"longitude": _surface_longitude(planet, d),` — additive only, the
   four existing keys (`house`, `sign`, `dignity`, `retrograde`) are
   untouched, in their original order, unchanged values.

**A second, unrelated occurrence of the identical `planetary_positions`
dict-comprehension shape exists in `build_varshaphal_chart()`
(line ~1021)** — explicitly documented in that function's own comment
as "Duplicated from calculate_chart()'s Lagna/planet block (not
extracted...)." This was correctly identified as OUT OF SCOPE (task:
"Edit ONE production function") and left untouched; the `Edit` tool's
first attempt matched both occurrences and was rejected (2 matches, not
1) before more context was added to target only the block inside
`calculate_chart()` (anchored on the following `"conjunctions":` line,
unique to `calculate_chart()`).

`chart_d1.py` stub was not opened or touched, per constraint.

## Step 3 — regression gate

- `pytest tests/regression/test_chart_calculator_characterization_
  david.py -v` → **16 passed**, 0 failed. All four pre-existing
  per-field/per-planet assertions (house/sign/dignity/retrograde)
  unaffected by the new additive key — none of them enumerate an exact
  key set, so adding `longitude` did not break anything.
- `pytest -q` (full suite, ran in background due to a 120s CLI timeout,
  completed in 149.53s) → **3954 passed, 7 skipped, 0 failed, 1
  warning** (a pre-existing, unrelated `opentelemetry` deprecation
  warning) — **byte-identical to the pre-edit baseline** (3954/7/0
  from the prior 1a-lock task). No test broke; no STOP condition
  triggered.

## Step 4 — measure vs JHora (David), report only, no assertion

**Source:** `reference/oracle_fixtures/david.md` §3c ("JHora v8 — same
longitudes, absolute-degree form used by the Jaimini karaka oracle,"
sub-arcsecond precision, Session 54/57, ratified as the non-dasha
oracle per Gap O1) — present and parseable; no STOP needed. §3c gives
Sun/Moon/Mars/Mercury/Jupiter/Venus/Saturn/Rahu in `Sign Deg°Min'Sec"`
form; Ketu is derived as Rahu+180° (the codebase's own standing
convention, confirmed at `agent/chart_calculator.py` line ~421 and
cross-referenced in `tests/fixtures/jhora_sulabh.md`'s Ketu row per
earlier sessions) — not independently listed in the source table.

**Convention alignment:** both sides converted to absolute 0-360
sidereal degrees (JHora's sign+DMS parsed via the project's own
`SIGNS` ordering; production's new `longitude` field is already in
this form) — chosen over degree-in-sign because production's new field
IS in this form.

**Per-planet production vs JHora, David:**

| Planet | Production (abs °) | JHora (abs °) | Delta (arcsec) |
|---|---|---|---|
| Sun | 275.438209 | 275.459433 | -76.41 |
| Moon | 131.657901 | 131.673397 | -55.79 |
| Mars | 51.210714 | 51.226097 | -55.38 |
| Mercury | 282.746830 | 282.758158 | -40.78 |
| Jupiter | 353.906423 | 353.925589 | -69.00 |
| Venus | 238.738271 | 238.762203 | -86.16 |
| Saturn | 96.033649 | 96.045250 | -41.76 |
| Rahu | 204.739568 | 204.755014 | -55.61 |
| Ketu | 24.739568 | 24.755014 | -55.61 |

**MAX |delta| = 86.16″ (Venus). MEDIAN |delta| = 55.61″.** Every delta
is negative (production trails JHora by 41″-86″ on every body, no sign
flips) — a fixed-direction, sub-90″ offset, consistent with (though not
individually re-verified against) `agent/chart_calculator.py`'s own
`is_boundary_sensitive()` docstring naming an "irreducible discrepancy"
of a similar order between pyswisseph and the commercial-software
camp, and with `docs/KNOWN_DIVERGENCES.md` Gap A1's documented
15-20″-scale pyswisseph-vs-JHora gaps in a different (True Chitra)
mode. This measurement is reported for human tolerance-setting only —
**no tolerance-band assertion was written**, per the task's explicit
"do NOT create a tolerance-based assertion" instruction.

## Scope confirmation

`git status --short -- agent/ tests/ data/` after all work:
```
 M agent/chart_calculator.py
?? data/_backup/ ...  (pre-existing, unrelated untracked pile)
```
Only `agent/chart_calculator.py` was modified; nothing under `tests/`
was touched; no new fixture created; `chart_d1.py` stub untouched.
**Not committed.**

---

Additive field surfaced, regression-clean, deltas measured.
AWAITING HUMAN REVIEW of the delta table before setting a validation
tolerance or committing.
