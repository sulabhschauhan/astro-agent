# S129 step 2 — planet positions into the fact block (chart_d1 restatement)

Branch wip/interpretive-pilot, on top of cc297c2. UNCOMMITTED.
NO live LLM call was made. Planner and interpreter stubbed throughout.

## The framing correction that changed the job

`chart_d1` was NOT implemented and must never be. Verified against the record:
the stub is a Phase-0 placeholder (`f5cf073`), and the refactor that would have
filled it (P1.1/P1.4, shared `Chart` dataclass) was ABORTED BEFORE ANY CODE WAS
WRITTEN at S22/S24 (`SESSION_LOG_ARCHIVE_S19-S66.md:73`). The D1 chart is in
production in `chart_calculator.calculate_chart()`. This work RESTATES it.
Recorded in 3 places so no session repeats the mistake: the stub's own
docstring, CLAUDE.md's DESIGN-INTENT-FIRST list, KNOWN_PATTERNS P-022.

## Measurement that set the scope (done BEFORE any code)

Corpus, whole 2-book, 20,426 sentences:
  planet-in-house shape   344 sentences   94.5% survive the 4 disqualifiers
  lord-in-house shape     176 sentences   (the shape gated today)
  planet-in-SIGN shape     57 sentences   -> deferred, own axis
  ambiguous (both shapes)   5 sentences   -> must return None
  retrograde/vakri-keyed   10 sentences   0.05% -> DROPPED from scope

Against the real 2026-09-12 gpt-5 run: 0 of 27 `silent_on` entries and 0 of 4
claims would newly be judged. Expected -- the block had no planet facts, so the
model made no planet claims. The corpus numbers are what size the payoff.

## Scope shipped: house + sign only

NO dignity (no oracle table; 3-way source divergence; a false premise the gate
cannot catch). NO longitude (no verse keys on a raw degree). NO retrograde
(three grounds, see docs/PROJECT_FACTS.md S129 entry).
Rahu/Ketu ARE included as positions -- BPHS ch.34 v.16 states their effect
travels through the house they occupy, which is exactly this channel.

## Oracle validation: 36/36

Planet sign+house vs the RATIFIED §3e matched-mode JHora oracle:
  SULABH   lagna Sagittarius (oracle 3e)  9/9
  SURBHI   lagna Libra       (oracle 3e)  9/9
  DAVID    lagna Virgo       (production; no oracle lagna row exists)  9/9
  SHERIDAN lagna Taurus      (production; no oracle lagna row exists)  9/9
CAVEAT: David and Sheridan have no Lagna row in §3e or §1, so their SIGNS are
oracle-matched but their HOUSES are derived from our own ascendant. Only Sulabh
and Surbhi are end-to-end oracle-anchored.

CORRECTION OF RECORD: the S129 step-1 stress test used FABRICATED birth data
for Surbhi ("14 Oct 1992, Delhi"). The real values are 11 Sep 1992, 10:30,
Patna (`reference/oracle_fixtures/surbhi.md` §1, and every existing test).
The earlier 56/56 result stands for plumbing but one of its four charts was
not Surbhi. Re-run here with correct data.

## The silence gate: ADVISORY, not enforcing

`read_planet_condition` / `judge_planet_claim` mirror the lord reader exactly
(same sentence split, same four disqualifiers) and RECORD a verdict without
ever removing a claim. Reason: the lord reader earned drop authority from six
MEASURED wrong-drop classes (S125); no such measurement exists for this shape
and cannot until live runs produce planet claims. `stats` now carries
`advisory_applicable/not_applicable/undetermined`, `planet_reader_mode`, a
per-claim `advisory_detail`, and the promotion metric
`advisory_would_drop_a_kept_claim`.

Two findings recorded rather than fixed, both failing SAFE:
 - The enforcing lord reader does not match "the lord of the 3rd" phrasing,
   only "the 3rd lord". Unjudged -> kept. Widening a matcher that can DROP
   needs the S125 evidence bar.
 - The planet reader needs ordinal form ("the 4th"), not "the 4".

## Stress test

56 runs (4 charts x 7 question shapes x 2 interpreter modes), 0 exceptions.
Each "claims" run injected one TRUE and one FALSE planet claim: the advisory
reader flagged all 18 false ones (`advisory_would_drop_a_kept_claim`) and the
gate dropped none, which is the designed behaviour.

## Suite

before this step : 4028 passed, 9 failed, 7 skipped
after            : 4074 passed, 9 failed, 7 skipped   (+46 tests, 0 regressions)
Same 9 pre-existing failures (live OPENAI_API_KEY / ChromaDB corpus).
tests/calculations/compatibility/test_ashtakoot.py excluded from both --
it geocodes at module import, which this sandbox blocks.

## Owed

 - The Saturn "(R)" oracle conflict needs Sulabh's adjudication
   (docs/PROJECT_FACTS.md, S129 entry).
 - Pratyantar hook still owed on the fact-block path (no dasha in the block).
 - Architect's refactor NOT done: `pipeline._fact_block` still calls
   `payload_builder.parse_lord_house_map` for the lord map, so pipeline still
   reaches into a retrieval module for chart data. Recorded, not done --
   relocating it mid-arc is risk for zero behaviour change.
