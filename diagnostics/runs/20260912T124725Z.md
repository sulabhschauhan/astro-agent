# S127 Run Log — Oracle Reference Set Commit

Branch: `wip/interpretive-pilot` (confirmed before staging). Commit
only, no push.

## Commit

Hash: `25dd869`

Message: `docs(oracle): add matched-mode D1 oracle references for
david/sheridan/sulabh/surbhi (S127); JHora Traditional-Lahiri primary,
Camp-Y residuals <=54" documented (Gap A1/O1)`

## Staged paths (explicit, 4 files, no `-A`/`.`)

- `reference/oracle_fixtures/david.md`
- `reference/oracle_fixtures/sheridan.md`
- `reference/oracle_fixtures/sulabh.md`
- `reference/oracle_fixtures/surbhi.md`

`git diff --cached --name-only` immediately after staging listed
exactly these four paths, nothing else — no `git reset` needed.

## Sanity check (`git status --short` pre-commit)

Only the 4 files above appeared as staged (`A `, no working-tree
suffix). Everything else stayed unstaged: `CLAUDE.md`, `SESSION_LOG.md`,
`claude_handover_S126.md`, `docs/KNOWN_DIVERGENCES.md` (all ` M`), the
4 manifest files, all `diagnostics/runs/*.md` entries, and the full
untracked pile (`data/`, `scripts/`, `probes/`, other loose
`diagnostics/*` files) — all still `??`.

## Post-commit verification

- `git log -1 --oneline` → `25dd869 docs(oracle): add matched-mode D1
  oracle references for david/sheridan/sulabh/surbhi (S127); ...`
- `git status --short` → the 4 committed paths no longer appear at all
  (folded into HEAD); the same pre-existing `M`/`??` set from the
  sanity check is untouched — nothing else was swept in.
- No push performed.

## Result

`git commit` reported: `4 files changed, 2875 insertions(+)`, all 4
as `create mode 100644` (new files, matching the fresh-build/2nd-pass
history of this session — none of the 4 pre-existed before this
session's work). Commit is local-only on `wip/interpretive-pilot`.

AWAITING HUMAN REVIEW (no further action pending on this task).
