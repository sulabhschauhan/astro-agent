# S127 — commit the David characterization regression test (1a)

**Commit: `53b90f3`.** "test(regression): lock David characterization
for calculate_chart() public D1 fields (S127 1a); 16 golden fields,
drift-guard only, not an oracle"

## Step 0 — branch

`git branch --show-current` → `wip/interpretive-pilot` (confirmed).

## Step 1 — stage exactly one file

`git add tests/regression/test_chart_calculator_characterization_
david.py` (explicit path, not `-A`/`.`).

`git diff --cached --name-only` →
```
tests/regression/test_chart_calculator_characterization_david.py
```
Exactly one path, matching the intended file. No STOP condition
triggered.

## Step 2 — sanity check before commit

`git status --short` filtered to non-`??` (staged/tracked) entries:
```
 M CLAUDE.md
 M SESSION_LOG.md
 M claude_handover_S126.md
 M docs/KNOWN_DIVERGENCES.md
A  tests/regression/test_chart_calculator_characterization_david.py
```
The `M` rows are **unstaged** working-tree modifications (space in the
first status column = not staged) carried over from earlier tasks this
session — not touched by this task's `git add`. The only **staged**
entry (`A ` = staged addition) is the intended test file. Sanity check
passed; proceeded to commit.

## Step 3 — commit

`git commit -m "test(regression): lock David characterization for
calculate_chart() public D1 fields (S127 1a); 16 golden fields,
drift-guard only, not an oracle"` (plus the standing attribution
trailer) → **commit `53b90f3`**, "1 file changed, 97 insertions(+)",
`create mode 100644 tests/regression/test_chart_calculator_
characterization_david.py`.

## Step 4 — verification

- `git log -1 --oneline` → `53b90f3 test(regression): lock David
  characterization for calculate_chart() public D1 fields (S127 1a);
  16 golden fields, drift-guard only, not an oracle` — hash and message
  confirmed.
- `git status --short` → the test file no longer appears (fully
  committed, clean of it); the pre-existing `M`-modified files and the
  large untracked pile from prior sessions are unchanged and untouched.
- **Not pushed** — no push instruction was given, none performed.

---

Committed: `53b90f3`. One file, nothing else. Not pushed.
