# Known Patterns

Append-only index of settled diagnostic conclusions. Purpose: before starting
a new diagnosis, grep this file for the symptom first — if it's here, the
root cause is already settled and cited; re-derive only if the cited
evidence no longer supports the conclusion (verify the commit/session still
says what this row claims, per CLAUDE.md Working Style #12).

This file holds CONCLUSIONS only. Reasoning, probe methodology, and raw
numbers stay in SESSION_LOG.md / diagnostics/<topic>_S<xx>.md — do not
restate them here, only point at them.

NOTE ON COMMIT HASHES: the S82 history rewrite (force-push, git-filter-repo)
changed every commit hash dated S81 and earlier. Rows below for S79-S81
findings cite the CURRENT (post-rewrite) hash for the nearest still-valid
commit, verified to resolve at the time this file was written — not the
original hash printed in SESSION_LOG.md's own S79-S81 blocks, which is
stale and will not `git cat-file -t` successfully.

## Standing rules (not bug patterns)

- **Never set a retrieval/extraction tuning number from a hand-picked or
  synthetic sample.** Collect real production failures first (the dogfood
  capture net), set thresholds from that evidence. Ruling: S83 (parked
  `_N_RESULTS_PER_FEATURE` widening pending real silences), reaffirmed S84
  (live data vindicated window=3; widening stays parked).
- **"Absent from top-N search results" is not "missing from the corpus."**
  Establish existence by direct `collection.get(ids=[...])` lookup, never by
  whether a chunk appears in ranked output. Conflating these produced a
  false DATA verdict and three sessions of wrong direction (S81).
- **Validate vision-PROMPT changes on a real ground-truthed image, never the
  suite (fake client).** A pytest suite mocks the LLM client and cannot
  detect a real vision-model regression from a prompt edit — only a live
  run against a human-verified ground-truth image can. Ruling: S90.
- **Self-consistency is not correctness.** N-run agreement across repeated
  calls measures precision (the model is stable), not accuracy (the model
  is right) — a model can stably confabulate the same wrong answer every
  time. Ruling: S90.
- **Palm vision reads only clearly-visible lines.** Faint, occluded, or
  fold-dependent lines are NOT forced into a definite reading — honest
  silence; the astrology track covers the gap a flat photo leaves (a
  palmist sees more by folding/pressing/tilting the hand than any photo
  can show). Ruling: S90.
- **Origin/termination landmarks are a closed, book-derivable set per
  line.** Enumerate the possible values from the source text first and
  constrain vision to that per-line menu — do not build general
  open-world LLM origin/termination logic. Ruling: S90.
- **A generic ENGINE test must never borrow a live DOCTRINE rule as its
  fixture.** A test asserting `match()`/`resolve_priority()` mechanics that
  reaches into the real rule set (`BY_ID[...]`, a fresh `load_rule_set()`,
  or an exact real-data count/antecedent-shape assertion) couples engine
  correctness to doctrine content that has every right to change — a
  legitimate rule edit then breaks a test about something else entirely.
  Rebuild the fixture synthetically through the real `PalmRule`/`Antecedent`
  construction path (or the real loader over an in-test rule dict); never
  re-point it at a different real rule, which only relocates the same
  landmine. Ruling: S123, `97bbd01` (3 tests fixed this way); full list of
  the ~16 tests in `tests/interpretive/test_palm_rules_table.py` still
  coupled at that commit: see P-021.

## Patterns

| ID | Symptom (what a future session would observe) | Root cause | Status | Evidence (session #, commit) |
|---|---|---|---|---|
| P-021 | A legitimate, correct rule-file edit (e.g. S123's H_002 Head migration) breaks unrelated tests in `tests/interpretive/test_palm_rules_table.py` that were never about that rule's doctrine. | Test fixtures borrow a real production `rule_id` (`BY_ID[...]` or a fresh `load_rule_set()`) as a stand-in for a generic engine-mechanics case, or assert an exact real-data count/antecedent-shape — see the standing rule above. | PARTIALLY FIXED — 3 tests rebuilt synthetic (S123, `97bbd01`). 16 tests confirmed coupled at that commit by direct source read — corrects CLAUDE.md's carry-forward bullet, which names "roughly fifteen" and omits `test_priority_hl021_mirror_c2_suppresses_synthetic_general_rule`; CLAUDE.md's own itemization was left as-is (not edited) per this task's scope, this row is the authoritative count. Full list, name (line, real id/count depended on): `test_loader_loads_all_43_validated_candidates` (31, `len(RULES)==48`); `test_loader_emits_warning_only_when_unverified_rules_present` (35, `all(r.verified for r in RULES)`); `test_unknown_value_no_match_no_exception` (98, `BY_ID["H_002"]`); `test_unknown_feature_no_match_no_exception` (105, `BY_ID["H_002"]`); `test_compound_rule_fires_only_when_both_antecedents_observed` (216, `BY_ID["H_004"]`, exact 2-antecedent shape); `test_h010a_fires_when_head_depth_greater_than_heart_depth` (236, `BY_ID["H_010a"]`/`BY_ID["H_010b"]`); `test_h010b_fires_on_the_reverse` (242, same pair); `test_h010_neither_fires_when_magnitudes_absent` (248, same pair); `test_h010_neither_fires_when_magnitudes_partial` (253, same pair); `test_priority_hl006_survives_general_heart_rule_suppressed` (263, `BY_ID["HL_006"]`/`BY_ID["HL_011"]`, subset relationship); `test_priority_hl021_mirror_c2_suppresses_synthetic_general_rule` (281, `BY_ID["HL_021"]`, paired with a synthetic rule); `test_priority_never_suppresses_across_different_topic_groups` (315, `BY_ID["H_021"]`/`BY_ID["HL_011"]`); `test_priority_equal_antecedent_count_siblings_never_suppress` (333, `BY_ID["H_005"]`/`BY_ID["H_006"]`, sibling shape); `test_load_rule_set_real_data_merges_43_plus_13_with_unique_ids` (402, `len(merged)==87`); `test_load_rule_set_skips_candidates_subdirectory` (421, real id-set has no `R_`-prefixed id); `test_load_rule_set_baseline_field_present_and_correct` (432, `by_id["L_001"]`/`by_id["H_002"]` via a fresh `load_rule_set()`). Tests confirmed NOT coupled despite reusing a real-looking `rule_id` string (`test_baseline_hardest_case_non_subset_non_baseline_displaces_baseline`'s `_make_rule("L_001", ...)` etc.) — that section's own header comment states they pin baseline-suppression LOGIC independent of real rule content; excluded deliberately, not missed. | S123, `97bbd01` (fix); this task (source-verified reconstruction + filing) |
| P-020 | A vision-emitted relation_target landmark is silently dropped by the extractor (e.g. generic "Mount of Mars"). | Vocabulary drift — vision emits a generic landmark name absent from `relation_target_registry`, which holds only specific variants (Upper/Lower Mount of Mars). | RESOLVED-BY-DESIGN — Prompt 5a/5b enumerates the book's closed per-line origin/termination menu and constrains the vision prompt to name the specific registry variant; no generic alias needed. | S90, Prompt-4 run |
| P-019 | A known faint/varying-depth line segment (e.g. faint-upper fate line) never surfaces as anything but uniform depth across repeated vision runs. | Vision cannot read intra-line depth variation from a flat photo — reads the whole line as one uniform depth value. | SETTLED (honest silence) — do not force a depth-variation read; out of scope for a flat photo (see palm-scope standing rule). | S89 |
| P-018 | A vision-reported attribute never discriminates between hands/runs — every value comes back identical (e.g. `proximity_degree` always "medium"). | The attribute carries no real signal for this model/prompt; not an extraction bug. | SETTLED (honest silence) — drop the dead attribute from rule antecedents, do not chase a fix. | S89 |
| P-017 | Verified rules go silent (stop firing) immediately after an unrelated `attribute_value_binding` addition to the ontology registry. | The extractor's closed-vocabulary guard narrows to the new binding immediately; baked antecedent values that predate the migration fall outside the narrowed set and are dropped before any rule can match. | SETTLED — bind `attribute_value_binding` LAST, atomically with the rule+pool migration, never as a standalone early step. | S89/S90, `b62d1ef` |
| P-016 | Stage-1 extraction outcomes (`empty_first` raw=0, or overlap-floor rejection at 0.4) vary run to run for the same or similar feature/chunk pairing, with no fixed ground truth to check a given call's behavior against. | Each palm reading runs Stage-1 extraction live, per-request, against a runtime threshold (`_PARAPHRASE_OVERLAP_FLOOR=0.4`) with no offline-verified baseline — every call is a fresh judgment with nothing to calibrate or regression-test it against. | SUPERSEDING FIX IN PROGRESS — see CLAUDE.md V1.1 register, offline-verified-extraction pilot (Cheiro-scoped) | S84 `near_miss_margin` data; this session's `dogfood_capture.md` re-analysis (`diagnostics/latest_run.md`) |
| P-015 | A compound conditional claim spanning two features (e.g. head-line + heart-line combination doctrine) never gets surfaced — no single-chapter retrieval or extraction step can assemble it. | Doctrine for compound/cross-chapter conditions is genuinely split across chapters in the source text; the S82 page-range gate is chapter-scoped by design, so no per-chapter retrieval call can join conditions that live in different chapters. | SUPERSEDING FIX IN PROGRESS — same pilot, extraction re-scoped book-wide (not chapter-grouped) to handle this class | This session, `playbook_export/decisions/rule-engine-v1_5-design.md` addendum |
| P-014 | fate line / fingers / heart line fail production runs despite correct doctrine chunks present and well-scored in the candidate pool; one head-line failure occurred even with a STRONGER top-3 than passing runs of the same feature. | RULED OUT as a window/rank problem — 3 production runs post-S84 show zero buried-rank cases, no-cliff shape holds (rank-3 within 0.006-0.041 of rank-1 in every failing row); the stronger-top-3-yet-failing head-line case rules out rank position as the mechanism entirely. | RETIRED (do not re-open without new contradicting evidence) — superseded by the offline-extraction pilot (P-016), not further threshold work | `diagnostics/latest_run.md` (this session's capture-net analysis) |
| P-013 | Stage-1 claims for fate line / heart line / mount of jupiter / fingers come back `empty_first` or `unsupported` even though `near_miss_margin` shows correct doctrine already ranked in the top-3 window. | SUSPECTED, unconfirmed — `claim_extraction.py`'s `_PARAPHRASE_OVERLAP_FLOOR=0.4` rejecting valid claims on first attempt. Rank window explicitly ruled OUT as the cause for these same misses (S84 live check). Not yet root-caused. | OPEN (current lead) | S84, `3cc3abe` / `8a4881c`; reconfirmed this session — 3 more production runs show `empty_first` (raw=0 extraction despite the correct chunk already in-window) plus thumb's overlap rejected at 0.31/0.38 vs the 0.4 floor, recovered only on retry (`diagnostics/latest_run.md`) |
| P-012 | A feature's correct in-chapter doctrine chunk ranks 4th or worse inside the gated candidate pool and is discarded before Stage-1 ever sees it (heart-line rank sweep: 3 of 15 rows buried at ranks 4-8). Re-observed in a live head-line/"deep" query where genuine head-line chunks (`p146_c*`, most of `p147_c*`) rank outside the top-3 while a chapter-opening epigraph chunk occupies rank 1. | `_N_RESULTS_PER_FEATURE=3` is too narrow for SOME features under the page-range gate; NOT confirmed as the cause of any current production failure — S84's live-data check found the opposite (window already holds the correct/strong candidate for every failing feature it reviewed). Two facts coexist: the failure mode is real or the controlled sweep, current production incidents are not window-caused. | PARKED (widening deferred pending real production silences — do not set from the S83 sweep sample; see standing rule above) | S83, `4602398` / `1ec6666`; re-confirmed S84, `3cc3abe` |
| P-011 | Palm reading voices a neutral, disjunctive classical passage ("intellectual strength OR weakness") as a one-sided positive claim ("indicating intellectual strength"). | Stage-1 valence extraction (and Stage-2 voicing) labels definitional/two-sided doctrine `valence="supports"` with no polarity check. NOT a retrieval defect — retrieval delivers the correct chunk (`p145_c0`) correctly and in-range; do not re-diagnose this as a retrieval-completeness problem. Layer: PROMPT / Stage-1. | OPEN (E-5 fail-closed detector drafted this session in `claim_extraction.py`, uncommitted, awaiting `RATIFIED: commit authorized` per Working Style #14) | S71, `8f39a5b`; reconfirmed S81, `6391cce`; E-5 draft: `diagnostics/latest_run.md` (uncommitted) |
| P-010 | Any attempt to ground an Indian-tradition (non-Cheiro) palmistry claim finds no usable corpus text; palm retrieval grounds on Cheiro (Western tradition) only. | Hasta Samudrika Shastra corpus is a photograph of an open book: 449 pages, only 4 with >=50 alphabetic words, zero Devanagari codepoints anywhere — needs page-split + dewarp + Devanagari OCR + translation, none of which is built. | WONT-FIX as-is (re-sourcing an English edition or a full Devanagari OCR+translate pipeline is the only fix path; not attempted) | S80 (F10 finding), `5fbf8b7` / `76352b8`; re-measured S81, `54c3517` |
| P-009 | A whole page's worth of corpus text is missing even though the source PDF's native text layer has it (Jyotish Lal Kitab 416 pages, similar pattern suspected in Lal Kitab-1941 / Jataka Parijata). | `classify_page()` over-fires "mixed" on ordinary planet-dense prose (`PLANET_MATCH_THRESHOLD`); `pdf_processor.py` discards OCR text for any "mixed" page and `image_extractor.py` never back-fills mixed pages (only "diagram"). Traced source-to-source, 100% reproducible on the sampled book. | OPEN — root-caused, not yet fixed or re-ingested; the 75.9% sizing figure used to scope this was itself VOID (see P-007) so true corpus-wide scope is still unmeasured | S80, `5fbf8b7` / `76352b8`; void-census finding S81, `54c3517` |
| P-008 | Retrieved chunk text for palm/astrology-corpus features contains systematic token corruption (e.g. "line" -> "hne", "life" -> "hfe"). | Ingestion pipeline discards each PDF's native embedded text layer and re-OCRs with Tesseract; ~104 corruption tokens corpus-wide. Confirmed non-causal for the specific fate-line/head-line retrieval failures investigated (S81 layer table assigns those to RETRIEVAL/PROMPT, not corpus repair). Page-range gate (S82) raises exposure to this corruption by shrinking the competing candidate pool for gated features — direction certain, magnitude unmeasured. | OPEN (repair path is Path D re-ingestion; not started; non-blocking for the current palm ship) | S79, `76352b8`; corruption census + Path D lock S80, `5fbf8b7`; gate-interaction note S82, `0334038` |
| P-007 | A corpus-completeness percentage (e.g. "75.9% native text coverage") gets cited as fact and used to size a remediation decision. | `data/progress/*.json` covers only 14 of 22 books and has no `native_text_present` field; the cited percentage was computed over an unknown-completeness subset with a broken exclusion term. The pre-registered decision cut never actually fired. | RETIRED (verdict voided; any future corpus-wide sizing must read PDF page counts directly, never `progress.json`) | S81, `54c3517` |
| P-006 | A chunk that never ranks in top-N search results (e.g. `p146_c2`, `p147_c0` for head line; `p159_c2`/`p160_c1` for heart line) gets carried for multiple sessions as an "accepted retrieval gap" / corpus defect. | The original "correct chunk" target list (S68-era) was never independently validated. Direct re-verification found the allegedly-missing doctrine chunks either rank correctly once chapter-scoped, or the "top-ranking" chunk that displaced them (e.g. `p145_c0` at rank 6 for head line) is itself CORRECT behaviour (chapter-opening epigraph, not competing sign doctrine). | RETIRED (old accepted-gap framing superseded; do not re-inherit the "never ranks" framing) | S81, `6391cce`; CLAUDE.md accepted-gap (c) reframe, `4070dba` |
| P-005 | Palm retrieval "misses" (a target chunk not appearing in search results) get read as evidence of a corrupt or incomplete corpus. | Conflated "absent from top-N ranked results" with "missing from the corpus" (see standing rule above). Direct `collection.get(ids=[...])` lookup showed every named target chunk exists; zero empty TEXT pages across the whole Cheiro doctrine range p133-182 (all 9 empty pages are `page_type='diagram'` plates). | RETIRED (corpus exonerated for palm; prior DATA verdicts and all ranks computed under the conflated method are VOID) | S81, `6391cce` / `4070dba` |
| P-004 | A chunk-boundary "swap in the native-aligned text span" repair strategy (Path C) is proposed to fix OCR corruption. | Foundered on measurement: 210 tokens where the NATIVE text itself is corrupt (a blind "prefer native" rule would inject new errors), plus 322 mid-page tokens that require INSERTION into chunk boundaries frozen by the Ring 3 rubric anchors — can't be done without breaking those anchors. | RETIRED, replaced by Path D (re-ingest from native text, boundaries reseeded, Cheiro-first pilot) | S80, `5fbf8b7` / `76352b8` |
| P-003 | A single high-scoring chunk (`cheiroslanguageo00chei_1_p123_c0`, a table-of-line-names/nomenclature passage) ranks #1 for head-line queries and #7 for heart-line queries, ahead of any real doctrine for that line. | The chunk names every classical line by its alternate name, so it scores well against ANY line-quality query; nothing in the unfiltered retrieval path scopes it out of a specific line's chapter. | RETIRED for the 9 features now covered by the S82 page-range gate (contained — post-gate head-line sources resolve to p145, not p123); LATENT/LIVE for any feature that ever ships without a verified page range | S82 (three-arm probe), `0334038`; live containment re-check S83, `4602398` |
| P-002 | Near-miss chunks from unrelated chapters (other lines' doctrine, or the nomenclature table P-003) fill the top-k retrieval slots for a palm feature, displacing that feature's own doctrine. Adjacent-rank score gaps of only 0.016-0.034 with no cliff mean depth alone (raising `n_results`) cannot fix it — measured `min_n_all`: fate=15, head=NEVER, heart=NEVER. | No chapter/page scoping in retrieval — all ~463 Cheiro chunks compete for every feature query regardless of which chapter the feature's doctrine lives in. | SHIPPED — fixed by a Chroma `where`-clause page-range filter (`page_ref` `$gte`/`$lte` as two `$and` clauses), pushed into the SAME single `search()` call so the one-call-per-feature contract is preserved. No-op on 6 of 8 measured features, corrective on head line + fingers. | S81 (root cause + fix design), `6391cce`; S82 (shipped ON), `0334038` / `0dee13f` |
| P-001 | A retrieval-depth widening ("let the LLM choose from a deeper window", e.g. n=10) is proposed to fix out-of-chapter chunks polluting results. | Refuted on text, not argument: the WIDE arm's extra results are genuinely other lines' doctrine that never names its own subject ("If the line leave the line of life...") — chapter provenance is metadata, not recoverable from the chunk text itself, so depth cannot substitute for a range gate. Also 3.3x token cost per reading. | RETIRED (do not re-propose without new evidence that provenance became text-recoverable) | S82 (three-arm probe), `0334038` |

## Adding a row

New ID = highest existing ID + 1. Newest row goes at the TOP of the table
(table is newest-first; append above P-0xx's current top row, not below).
One line each for symptom / root cause / status — if you need a second
sentence, the reasoning belongs in SESSION_LOG.md, not here.
