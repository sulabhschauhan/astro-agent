# Ring 3 pass 3 chunk-text evidence dump -- delta report

Generated by `scripts/probe_pass3_chunks.py` (diagnostics-only, throwaway,
read-only). Full evidence surface: `diagnostics/ring3_chunks_S67_pass3.md`.

## Step 1 -- enumeration
Exactly 3 `## RUN` blocks dated 2026-07-18 in `.claude/read_prompt.md`
(lines 211, 307, 405). No ambiguity: A/B (`11:34:32` / `11:35:40`) share
byte-identical LEFT/RIGHT text (regenerate pair); C (`11:38:22`) is the
only block carrying a `#### HAND_DETAIL` subsection.

## Step 2 -- reconstruction gate
**Both shapes PASSED.** Run A/B (24 gated rows) and Run C (24 gated rows):
feature/page order exact, every score within the reconstructed-vs-captured
delta budget (max observed drift 0.0001, e.g. p.139 life line: captured
0.6108 vs. reconstructed 0.6107 -- well under the +/-0.0002 jitter
precedent). `supported_features`/`unsupported_features` tuples matched
captured exactly for both shapes. No STOP condition triggered.

## Step 3/4 deltas vs. Ring 3 pass 2 baseline (`ring3_chunks_S66_pass2.md`)
- **p.163 fate-line valence sentence is now IN the gated set for ALL
  THREE runs** (A, B, and C) -- pass 2's whole-description query never
  retrieved it at all. Full text confirmed present, doctrine content
  intact (strong/rising fate line = "success and riches...won by personal
  merit"; low/faint = "sacrificed to the wishes of parents or relatives").
  This is the single biggest structural change from pass 2: the doctrine
  chunk most relevant to this fixture's barely-visible-left/moderate-right
  fate line is now actually retrieved and gated in, where pass 2 silently
  never saw it.
- **mount of jupiter**: unsupported in Run A/B (no HAND_DETAIL -> no
  jupiter-naming source text -> correctly skipped, not queried) --
  supported in Run C once HAND_DETAIL's "Mounts: ...Jupiter...appear
  slightly raised" enters the feature-text gather step. Matches the S67
  R1 hand_detail-inclusion design intent exactly.
- **Doctrine-marker lookup**: unlike pass 2 (where only the life line's
  p.134 chunk carried genuine interpretive content across all 3 runs),
  this pass-3 fixture's gated set has at least one doctrine-marker hit
  for every one of the 9 supported features in both shapes -- fate line,
  heart line, thumb, fingers, mount of venus/jupiter, markings all now
  carry `denotes`/`indicat`/`tells`/`such a`-style sentences, not just
  naming/positional text. (Lookup only, not a scored judgment -- Ring 3
  pass 3's own human rubric still decides which of these are genuinely
  load-bearing vs. incidental.)
- **Run C delta pattern**: every feature except fate line picked up at
  least one NEW chunk in the Run C shape vs. Run A/B (HAND_DETAIL's extra
  field text shifts the per-feature query's quality string enough to pull
  a different top-3). Fate line is the one exception -- identical 3-chunk
  set in both shapes (HAND_DETAIL's fate line field was itself "Not
  clearly visible," contributing no new query signal).
- Two OCR-garbling artifacts preserved verbatim in the dump, same class
  as the pass-1/pass-2 precedent (`life`->`hfe`): p.111's mount-of-venus
  chunk has scrambled mid-sentence characters ("develoyeout", "iavee",
  "cove*®"), and p.98's fingers chunk has "eutt, hard, dry" for "cut,
  hard, dry" — noted for the human scorer, not corrected here.

## Not yet done
Ring 3 pass 3's own human-rubric scoring (P1-P4, P7, rubric table,
verdict) is NOT part of this task -- this is evidence-surface prep only.
